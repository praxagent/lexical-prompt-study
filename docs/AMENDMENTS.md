# Plan amendments

The locked charter may change only through an entry added here before the
affected outcome is inspected.

| ID | Date | Status | Outcomes inspected? | Change | Rationale | Approval |
|---|---|---|---|---|---|---|
| A000 | 2026-07-25 | Baseline | OpenRouter exploratory outcomes only; no raw-checkpoint target outcomes | Lock Study Charter v1.0 | Establish an auditable starting point before implementation or paid compute | Human requested first plan commit |
| A001 | 2026-07-25 | Pre-outcome design change | No raw-checkpoint target outcomes | Validate HarmBench on JBB's 300 published human-majority comparisons using balanced accuracy and recall as gates; report precision without gating it; use the runtime affirmative-token probability for the paired primary score | Published HarmBench binary labels pass balanced accuracy (0.802) and recall (0.873) gates but have precision 0.653, so a predeclared 0.80 precision gate would reject the evaluator while discarding its useful paired continuous signal | Human authorized execution under the locked change-control process |
| A002 | 2026-07-25 | Pre-outcome implementation clarification and infrastructure correction | No raw-checkpoint target outcomes | Resolve the HarmBench continuous score as the exact one-token `Yes` versus `No` softmax; retain both turns for E02; persist figure byte-verification status; verify Git blob IDs for non-LFS artifacts; bind restricted generation files to trial receipts; reserve H200 KV-cache headroom with the previously validated 125 GiB-per-GPU sharding ceiling | Remove implementation ambiguity and make the frozen evaluator, visual contract, artifact inventory, crash-resume evidence, and paid topology fail closed without changing any estimand, split, threshold, arm, or prompt | Human authorized execution under the locked change-control and provenance policies |
| A003 | 2026-07-25 | Post-generation environment correction | Target generations complete but raw outcomes and evaluator scores not inspected; failed scorer and tokenizer smoke wrote 0 score receipts | Add the `sentencepiece` and `protobuf` runtime dependencies required to load and convert the frozen HarmBench Llama-2 tokenizer, then retry the unchanged scorer against the same generation receipts and output root | The first scorer invocation and first corrected tokenizer smoke failed during tokenizer construction before loading the evaluator or writing a score; this repairs the locked environment without changing prompts, outputs, evaluator weights, scoring code, estimands, or thresholds | Authorized operational recovery within the approved study protocol |
| A004 | 2026-07-25 | Post-score analysis implementation correction | All evaluator receipts exist and pass linkage checks, but raw outcomes were not inspected and no gate statistic was produced | Retain the generation receipt's `turn` field in the gate analyzer's joined provenance row, bind the gate artifact to the analyzer implementation hash and source commit, and cover the complete 20-behavior/160-receipt discovery topology in a regression test | The analyzer already selected turns 1 and 2 but omitted `turn` from the constructed row before filtering for the frozen turn-2 estimand; adding the missing field and provenance identities repairs execution without changing inputs, scores, bootstrap seed, thresholds, or estimand | Authorized fail-closed recovery within the approved study protocol |
| A005 | 2026-07-25 | Pre-mechanism operational freeze | Gate-1 and Gate-2 derived behavioral scores inspected; no mechanistic outputs and no raw generated text inspected | Resolve the primary J-lens statistic as vocabulary-z-scored mean refusal-probe logit minus mean compliance-probe logit at the turn-2 assistant boundary; freeze secondary generated-token indices 0, 1, 2, 4, 8, and 16, where index k includes generated tokens 0 through k and reads the residual at token k; evaluate every declared lens source layer under fitted J-lens, identity, and a deterministic per-layer dense Gaussian transport Frobenius-matched to the fitted Jacobian using base seed 20260725; run SAE discovery only on the discovery split at residual-post layer 50; rank positive full-minus-structural-sham paired activation deltas by paired mean divided by root-mean-square paired delta, subject to at least 10% full-arm prevalence; retain one primary and at most three secondary candidates; preserve decoder norm, prevalence, sparsity, and reconstruction diagnostics | Make the descriptive readout and discovery selection executable and fail closed before any mechanistic outcome exists, while retaining the charter's estimand, controls, split separation, narrow descriptive interpretation, and held-out causal gate | Authorized operationalization within the approved Gate-3 protocol |
| A006 | 2026-07-26 | Pre-mechanism CLI wiring correction | First bounded qualification invocation stopped at Python argument binding before reading the private plan, hashing artifacts, loading a model, or writing any output | Pass the artifact-manifest path to `run_mechanism_discovery` rather than the unrelated `run_behavior` call | The artifact hash gate was implemented and tested at function level, but its CLI argument was wired to the wrong call site; this corrects invocation only and does not change observations, transforms, statistics, controls, selection, or thresholds | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A007 | 2026-07-26 | Pre-readout qualification correction | The bounded retry hash-verified inputs, loaded the model, and wrote derived residual-state checkpoints, but stopped at the vocabulary-moment fixture before writing any J-lens, identity, random-control, SAE, or summary output; no activation values or raw generated text were inspected | Evaluate the direct full-vocabulary fixture with dtype-compatible RMSNorm/unembedding operations; separately gate the float32 analytic equivalence and the actual bfloat16 module path; bind captured-state checkpoints to source commit, plans, artifact hashes, model/tokenizer revisions, and run ID | The first fixture passed activation capture but supplied a float32 vector directly to a bfloat16 linear module. The correction makes the intended validation executable and prevents a checkpoint created by different code or artifacts from being resumed | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A008 | 2026-07-26 | Pre-receipt schema correction | Qualification v2 passed artifact, capture, and vocabulary-moment gates and began the first in-memory transport batch, but stopped before writing any layer receipt, SAE diagnostic, or summary; no margin, activation, or raw-text value was inspected | Add the already-produced `observation_id` and behavior `category` provenance fields to the strict mechanism receipt schema, remove the redundant misnamed `observation_sha256` alias, and use `observation_id` for resume-topology validation | The runner correctly included observation ID and category in every receipt candidate, while the strict schema omitted them and therefore failed closed before persistence. The correction aligns schema with the frozen figure contract without changing any computed value | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A009 | 2026-07-26 | Pre-SAE-selection diagnostic correction | Qualification v3 wrote all bounded J-lens/identity/random layer receipts, whose values were not inspected, then stopped before writing SAE diagnostics or a run summary because the real SAE contains at least one zero-norm decoder column | Retain zero decoder norms as valid diagnostic observations but make them categorically ineligible for candidate selection; continue to reject negative/non-finite norms and negative/non-finite ReLU activations | A zero decoder column is a valid dead/degenerate feature diagnostic, not evidence of corrupt weights, but it cannot define a steering direction. Explicitly retaining and excluding it is stricter and more informative than aborting the whole SAE map | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A010 | 2026-07-26 | Prospective Gate-4 discovery-calibration freeze | Gate-1 and Gate-2 behavioral results and Gate-3 derived J-lens/SAE results inspected; no causal-intervention generation or held-out intervention outcome exists, and no raw generated text was manually inspected | Freeze primary SAE feature 10146; named secondary features 44802, 4057, and 3907 outside the primary family; matched controls 26453, 9105, and 40804 with the imperfect prevalence match disclosed; three seeded isotropic controls; direct normalized decoder-direction addition at residual-post layer 50 on the current final token at every decoding step; a discovery-only residual-norm-scaled rho ladder with deterministic sign/alpha eligibility and stopping rules; the 15-condition held-out attack topology; the 2-condition utility topology; paired base-behavior estimands, bootstrap gates, Holm control family, raw-outcome restrictions, exact receipts, and compute launch gate in `plans/gate4_intervention_v1.public.json` | Convert the discovery result into an executable, falsifiable causal protocol before any intervention outcome is produced, prevent confirmatory strength/sign selection, retain causal specificity controls, and separate a prompt-local causal claim from the stronger defense claim | Authorized prospectively by the user's approval to continue under the locked stage-gate and budget policy |
| A011 | 2026-07-26 | Pre-intervention numerical-semantics correction | No causal-intervention generation or held-out intervention outcome exists; a seeded local synthetic bfloat16 tensor fixture only | Define realized intervention norm as the effective float32 difference between post- and pre-residual values after the addition is cast to the model residual dtype; relax the requested-versus-realized relative tolerance from 0.1% to 5%, while retaining the separate hard 5% delta-to-pre-residual norm budget and forbidding clipping | The synthetic 8,192-dimensional bfloat16 fixture showed 0.47%-4.46% effective norm error across the frozen rho ladder because small coordinate updates are quantized when added to bfloat16 residuals. A 0.1% gate would reject the declared runtime representation rather than detect implementation drift. Recording the effective vector and allowing 5% is strict enough to catch material mismatch without pretending bfloat16 addition is exact | Authorized prospective numerical clarification under the locked Gate-4 change-control policy |
| A012 | 2026-07-26 | Pre-signed-output qualification diagnostics | Gate-4 qualification v1 wrote two zero-intervention receipts and stopped on the first signed prefill before producing a signed token or receipt; raw zero outputs and behavioral content were not inspected | Include requested norm, effective realized norm, relative error, and effective delta-to-pre-residual ratio in the fail-closed norm-mismatch exception | The 5% gate correctly stopped an under-resolved bfloat16 addition, but the exception omitted the derived numeric diagnostics needed to distinguish an implementation error from an unrepresentable low dose. Adding non-text norm diagnostics changes no vector, output, estimand, threshold, or selection rule | Authorized fail-closed diagnostic recovery within the prospective Gate-4 protocol |
| A013 | 2026-07-26 | Pre-signed-output bfloat16 tolerance calibration | Qualification v2 reproduced two zero-intervention receipts and stopped on the first signed prefill before producing a signed token or receipt; only derived norm diagnostics were inspected | Increase the requested-versus-effective realized norm tolerance from 5% to 10%; retain the effective post-minus-pre definition, exact per-step recording, forbidden clipping, and the independent 5% effective delta-to-pre-residual norm ceiling | On the actual B200/model/SAE path, the smallest frozen dose requested norm 0.0533935 and realized effective norm 0.0563576, a 5.551% mismatch, while the effective delta remained only 0.3324% of that event's pre-residual norm. A 10% representation tolerance admits this minor bfloat16 quantization difference without weakening the stricter residual-budget ceiling | Authorized prospective numeric calibration under the locked Gate-4 protocol before any signed output exists |
| A014 | 2026-07-26 | Post-outcome reporting addition | All Gate-4 discovery calibration receipts and derived scores inspected; selector returned no eligible alpha; held-out confirmatory outcomes remain unopened | Add a provenance-complete discovery calibration stop figure showing the four frozen signed half-span estimates and intervals, directional components, and runtime safety diagnostics from the immutable Gate-4 analysis artifact | Make the negative stage-gate result visually inspectable without changing a statistic, threshold, candidate, selection decision, or inference; the contracted held-out E06 and E07 figures remain absent because confirmatory intervention was not authorized by the frozen selector | Authorized reporting-only work after the prospectively frozen stop rule fired |
| A015 | 2026-07-26 | Post-replay reporting and frozen disposition | The four-arm feature-10146 replay aggregate was inspected; no Llama 3.1 8B or Qwen target outcome exists | Promote the hash-gated four-arm aggregate, add a provenance-complete E05b figure, report `0/20` base, `14/20` inert-length, `0/20` structural-sham, and `20/20` full prevalence, and enforce the prospectively frozen retirement of feature 10146 from confirmatory detector, breaker, and defense claims | The replay failed only the non-full prevalence ceiling because inert length activated the feature. Reporting the complete matrix corrects the prior full-versus-sham interpretation without changing the frozen replay rule | Authorized reporting and execution under the approved follow-up protocol |
| A016 | 2026-07-26 | Prospective follow-up placement-factor freeze | Gate-3/Gate-4 and four-arm replay outcomes inspected; no Llama 3.1 8B or Qwen target outcome exists | Replace the vague alternate-position robustness label with an explicit crossed scaffold-placement factor: `ep_before_request` and `ep_after_request`. Hold content and token budgets matched across orderings within each arm; produce separate behavioral, SAE, and J-lens results; prohibit pooled estimation and pooled candidate selection; require one common detector and any causal site to satisfy ordering-specific gates by a maximin/conjunction rule; keep base and ordinary-benign reference observations shared but never duplicate-count them | Placement may change both attack behavior and internal routing. Freezing the two orderings before generation prevents a favorable ordering from dominating a pooled result or being chosen after outcomes | Human explicitly requested both orderings as a controlled factor before follow-up outcomes |
| A017 | 2026-07-26 | Prospective placement-review repairs | A016 was committed and independently reviewed at high reasoning; no Llama 3.1 8B or Qwen target outcome exists | Add component-level token stability and rendering assertions; make discovery viability a screen and calibration a two-test Holm family; freeze maximin/threshold formulas and ties; align causal Holm testing and sensitivity; freeze detector missingness, fixed denominators, attainable counts, and fixed-panel interpretation; define the two-order claim boundary; and enumerate placement-inclusive work units | The independent successor review found five compact ambiguities that could otherwise mix placement with tokenization, permit outcome-dependent selection, or make error control and missingness inconsistent. The repairs add no arm, model, or outcome and preserve the user's exact two-order, no-pooling design | High-reasoning placement review `9c1c7f7cb03337ea8c16dee4aa2b9a20e0fea5285b14024bbf37c72a7e1357f1`; all B01-B05 accepted, I01-I03 accepted |
| A018 | 2026-07-26 | Prospective G1 infrastructure substitution | The exact A40 request failed with `could not find any pods with required specifications`; provider inventory confirmed no task-owned pod and no target or qualification outcome exists | Substitute exactly one secure RTX A6000 48 GB for G1 at the live `$0.53/hour` rate, retain the 90-minute wall limit and 20-minute no-progress timeout, and revise the maximum qualification compute cost from `$0.66` to `$0.795`; automatic fallback remains forbidden | The A6000 is the least-cost currently reported secure 48 GB substitute with non-null stock. G1 measures engineering compatibility, throughput, and memory on a synthetic-safe request, so the substitution changes the infrastructure receipt and downstream cost estimate but no scientific arm, prompt, outcome, selection, threshold, or claim | Existing full execution authorization; fail-closed cost-statement clause in the frozen G1 plan |
| A019 | 2026-07-26 | Prospective G1 region-compatible infrastructure substitution | The A018 RTX A6000 request also failed with `could not find any pods with required specifications`; post-failure inventory again confirmed no task-owned pod and no target or qualification outcome exists | Substitute exactly one secure B200 for G1 at the live `$5.89/hour` rate, shorten the wall limit from 90 to 30 minutes and the no-progress timeout from 20 to 10 minutes, and revise the maximum qualification compute cost to `$2.945`; automatic fallback remains forbidden | Global stock indicators did not reflect allocatability with the retained `US-CA-2` volume, while this exact volume previously admitted a secure B200. The shorter cap bounds the premium-device exposure while preserving the synthetic engineering qualification and persistent setup checkpoint. This changes no scientific arm, prompt, outcome, selection, threshold, or claim | Existing full execution authorization; fail-closed cost-statement clause in the frozen G1 plan |
| A020 | 2026-07-26 | Pre-generation tokenizer-boundary implementation correction | Pinned artifacts were downloaded and hash-verified, but the synthetic placement preflight stopped before model loading or generation because the first renderer could not recover the standalone component token subsequences in either ordering; no qualification or target outcome exists | Wrap each immutable request/scaffold block with the same frozen Llama reserved boundary tokens 0 and 1 and separate the two blocks with reserved boundary token 2; retain the same component bytes and all existing equality, unique-offset, total-token, context, and assistant-suffix assertions | The real pinned tokenizer merges ordinary textual delimiters with adjacent content, while reserved special tokens create explicit tokenizer boundaries. A synthetic tokenizer check established exactly one identical request and scaffold subsequence and equal 75-token prompts in both orderings before this correction was frozen | Authorized fail-closed implementation recovery under G0/G1; no model state or outcome existed |
| A021 | 2026-07-26 | Synthetic qualification checkpoint-key correction | A020 passed render validation, loaded the pinned model, and completed the first synthetic-safe generation in memory, then stopped before persistence because the private checkpoint store correctly rejected underscores in the derived stage key; no raw generation was persisted or inspected, no mechanism output exists, and no target content was used | Normalize the two already-frozen placement labels to hyphenated private stage keys (`generation-ep-before-request`, `generation-ep-after-request`) and add an exact uniqueness/allowlist regression test; change no render, token, model, generation, mechanism, statistic, or public receipt field | Placement labels are valid scientific identifiers but were interpolated directly into a stricter filesystem checkpoint namespace. Deterministic key normalization restores the intended atomic resume path without changing an outcome or estimand | Authorized fail-closed operational recovery within synthetic G1 |
| A022 | 2026-07-26 | Pre-target decoding and capture implementation freeze | Synthetic G1 passed; no harmful/benign follow-up target generation or target-model state exists | Freeze each follow-up observation as one user turn followed by deterministic assistant generation with no system prompt, seed 0, `do_sample=false`, `use_cache=true`, an 8,192-token context ceiling, and at most 1,024 generated tokens; generate shared base/ordinary-benign references once; cross the three structured harmful arms and structured-benign arm with both placement orderings; capture every pinned J-lens source layer plus SAE layer 19 at the assistant boundary before the first generated token; checkpoint raw generations, reconstructive token IDs, and bfloat16 states privately after every observation | The placement review froze matched single-turn orderings but the executable decoding ceiling and capture ledger were not yet machine-bound in the public JSON. This resolves those implementation degrees of freedom before any target outcome and makes the 860-unit work ledger exact | Existing full execution authorization and G0/G1 stage-gate policy |
| A023 | 2026-07-26 | Pre-target assistant-suffix hash correction | Synthetic G1 passed under prompts that remain unchanged; no harmful/benign follow-up target generation or target-model state exists. A local target-runner fixture showed that a fixed last-eight-token hash can include the tail of the second component when the common epilogue is short | Define the frozen assistant-boundary suffix as every prompt token strictly after the final recovered request/scaffold component offset, including the common block terminator, epilogue, and chat-template assistant boundary; require that suffix to be non-empty and identical across orderings | Offset-relative hashing measures the intended invariant directly and cannot accidentally depend on which component is second. This changes no rendered byte, token, generation, state, estimand, or G1 engineering conclusion | Authorized fail-closed pre-target implementation recovery under A017's exact-offset requirement |
| A024 | 2026-07-26 | Prospective G2 discovery compute statement | Synthetic G1 receipts and timings inspected; real-tokenizer discovery/calibration render preflights passed; no harmful/benign target generation or target-model state exists | Authorize only the 140-observation G2 discovery generation/capture run on exactly one secure B200 at `$5.89/hour`, using the slower G1 placement rate of 21.410 generated tokens/second, a 150-minute wall limit (`$14.725` maximum), 15-minute no-progress timeout, and no automatic fallback | At 1,024 tokens for every trial, the conservative generation estimate is 6,695.9 seconds (`$10.96`), leaving roughly 30 minutes for exact verification, model load, state capture, receipts, and teardown. Calibration remains gated on the discovery behavioral screen and a separate cost statement | Existing full execution authorization; measured-throughput and per-run cost gates in the frozen plan |
| A025 | 2026-07-26 | Prospective G2 live-rate reconciliation | Immediately before launch, the provider's read-only inventory reported the secure B200 rate had moved from `$5.89/hour` to `$5.98/hour`; no G2 pod, target generation, or target-model state existed | Retain exactly one secure B200, the 140-observation discovery topology, 150-minute wall limit, 15-minute no-progress timeout, and no-fallback rule; update only the live rate and corresponding maximum compute from `$14.725` to `$14.95` | The live-rate change is a 1.53% infrastructure price movement and changes no prompt, placement, model, decoding rule, state capture, estimand, selection rule, or scientific claim | Existing full execution authorization; fail-closed exact-cost gate in A024 |
| A026 | 2026-07-26 | Pre-score follow-up evaluator adapter and bootstrap stream freeze | G2 generation/capture completed and its receipt/private bundle passed integrity validation; no raw generation or HarmBench score was inspected and no behavioral contrast was computed | Permit the pinned HarmBench scorer to resolve the follow-up receipt's `restricted_artifact_path` and partition-indexed private behavior rows; freeze discovery bootstrap base seed 20260726, use NumPy `default_rng(seed + order_index)` for the two separately reported ordering strata, and `seed + 2` for the descriptive paired placement interaction | The follow-up receipt and private-plan schemas intentionally differ from v1, while the evaluator model, exact Yes/No scoring rule, prompts, generated bytes, 10,000-replicate percentile bootstrap, thresholds, and no-pooling rule are unchanged. Freezing deterministic streams before scoring removes an implementation degree of freedom | Existing full execution authorization; pre-analysis fail-closed implementation recovery |
| A027 | 2026-07-26 | Pre-score G2 provenance binding and bounded scorer compute | G2 generation/capture and a no-model resume audit completed; 140 receipts, 140 restricted artifacts, and 140 state bundles were retrieved with matching deterministic bundle hash. Raw outcomes and HarmBench scores remain unopened | Bind the exact generation source/public/private plans, run ID, render preflight, bundle hash, and 140/140/140 topology in the public plan. Authorize only pinned HarmBench scoring of the 140 receipts on exactly one secure B200 at no more than `$5.98/hour`, with a 20-minute wall (`$1.9934` maximum), 10-minute no-progress stop, and no fallback | The same evaluator scored 180 prior intervention receipts in 49.28 seconds; linear scaling estimates 38.33 seconds for 140, leaving 19.36 minutes for allocation, exact verification, model load, receipt checks, retrieval, and teardown. Exact input binding prevents scoring a substituted discovery bundle | Existing full execution authorization; receipt-first stage gates and exact per-run cost policy |
| A028 | 2026-07-26 | Zero-score follow-up classifier-text key correction | The first A027 scorer invocation stopped before evaluator construction/load with `KeyError: behavior` and wrote 0/140 score receipts. Generation outputs and raw text were not inspected | Resolve classifier target text from the v1 private-row key `behavior` or the follow-up private-row key `request`, requiring one non-empty string; change no classifier prompt template, behavior bytes, generation bytes, evaluator, label tokens, scoring rule, threshold, bootstrap, or receipt | The follow-up plan intentionally labels the immutable behavior string `request`; the generic scorer adapter handled its partition and artifact-path schema but not this final key rename. A synthetic regression test covers both schemas before retry | Authorized fail-closed same-pod operational recovery within A027's original wall/cost cap |
| A029 | 2026-07-26 | Prospective G2 calibration statistics and generation cap | The receipt-backed discovery screen passed separately in both placement orderings and its public result was committed; no calibration generation, state, or score exists | Freeze 65,536 two-sided paired sign-flip draws with plus-one p-values at seeds 20260729 and 20260730 for before/after placement, Holm family alpha 0.05 with placement-order tie break, and a descriptive 10,000-bootstrap interaction at seed 20260731. Authorize only 140 calibration generation/capture observations on one secure B200 at no more than `$5.98/hour`, 45-minute wall (`$4.485` maximum), 15-minute no-progress stop, and no fallback; scoring remains closed until the completed bundle is hash-bound | Discovery generation/capture completed in 569.31 seconds. The 45-minute cap leaves 35.51 minutes for allocation, exact plan/artifact verification, model load, receipt validation, retrieval, and teardown while keeping the formal test implementation fixed before calibration outcomes | Existing full execution authorization; G2 stage gate and exact per-run cost policy |
| A030 | 2026-07-26 | Pre-score calibration provenance binding and scorer cap | Calibration generation/capture completed in 538.24 seconds; its 140 receipts, 140 restricted artifacts, and 140 state bundles passed a no-model resume audit and local/remote bundle-hash match. Raw outcomes and HarmBench scores remain unopened | Bind exact calibration generation source/public/private plans, run ID, render preflight, bundle hash, and topology. Authorize only pinned HarmBench scoring of 140 receipts on one secure B200 at no more than `$5.98/hour`, 20-minute wall (`$1.9934` maximum), 10-minute no-progress stop, and no fallback | Discovery scoring of the same receipt count completed in 37.27 seconds, leaving 19.38 minutes for exact verification, evaluator load, score validation, retrieval, and teardown. The analyzer must reject any calibration receipt outside the frozen binding | Existing full execution authorization; formal calibration gate and exact per-run cost policy |
| A031 | 2026-07-26 | Prospective G3 SAE/J-lens readout freeze | G2 behavioral discovery and formal calibration results were inspected separately for both placements; the 280 assistant-boundary state bundles exist, but no Llama 3.1 SAE activation, candidate identity, subspace, dense comparator, J-lens margin, identity margin, or random-transport margin has been generated or inspected | Freeze common-feature eligibility in both placements, maximin individual ranking, one single feature, one at-most-eight-feature subspace with inverse-worst-order-RMS positive L2-normalized weights, calibration selection between only those candidates, a secondary equal-order-weight dense mean-difference projection, exact inherited refusal/compliance token IDs and semantic hashes, all 31 lens source layers, fitted/identity/Frobenius-matched Gaussian transports, deterministic 10,000-bootstrap streams, private complete activation/per-observation receipts, and separate discovery/calibration × before/after reporting with no pooled placement estimate. Defer the common detector threshold until the ordinary-benign and structured-benign utility-calibration negatives exist | The prior protocol fixed the maximin selector but not how the subspace, probe replay, bootstrap streams, or incomplete-negative threshold boundary were constructed. This amendment removes those remaining degrees of freedom before mechanism readouts and prevents an incomplete harmful-only calibration matrix from being mislabeled as a deployable detector | Existing full execution authorization; G3 stage gate and raw-outcome restrictions. Paid readout compute remains closed until exact A031 source, input manifests, local tests, throughput basis, and a separate cost cap are committed |
| A032 | 2026-07-26 | Prospective G3 exact-input and compute authorization | A031 is committed and pushed at exact source `655fa9b69b185cfcbad5ce51fb027909c1d73d18`; all local tests pass; the discovery and calibration receipt/state manifests were validated without opening raw prompts, generations, or reconstructive token IDs; no G3 mechanism value exists | Bind the exact A031 runner source, both 140-state input manifests and private bundles, the inherited probe-plan hash, and the expected public/private receipt topology. Authorize only one secure B200 at no more than `$5.98/hour`, with a 30-minute wall (`$2.99` maximum), 10-minute no-progress stop, atomic resume, and no fallback | The synthetic-safe B200 qualification completed two observations and exercised model, SAE, J-lens, identity, random-control, and patch paths in 7.3718 seconds. Conservative linear scaling to 280 captured states is 1,032.05 seconds, leaving 12.80 minutes for exact verification, model/artifact load, vectorized analysis, receipt validation, retrieval, and teardown. Generation is not repeated | Existing full execution authorization; G3 exact-cost gate, current `$100` cumulative soft gate, and `$200` cumulative hard ceiling |
| A033 | 2026-07-26 | Post-SAE/pre-J-lens local-snapshot provenance correction | The exact A032 runner validated all 280 input states and wrote private mode-0600 SAE activation, unopened discovery diagnostics, and dense-direction checkpoints, then stopped before model/J-lens loading because a tokenizer loaded offline from the exact local Hugging Face snapshot had no `_commit_hash`. No feature identity/value, dense value, J-lens value, raw prompt, generation, or reconstructive token ID was inspected; no public result exists | Accept a missing loaded-object `_commit_hash` only when the canonical local snapshot directory basename exactly equals the pinned model revision; continue to reject any non-null mismatched object hash. Retain exact tokenizer semantic-hash checks, model topology checks, artifact hashes, input manifests, and all scientific rules | Hugging Face local-only loading does not reliably populate the optional object metadata even when the caller supplies the exact immutable snapshot directory. Binding the canonical path to the pinned revision plus semantic/topology checks restores the intended fail-closed provenance gate without changing or inspecting an estimand | Existing full execution authorization; fail-closed implementation recovery. Replacement compute remains closed until this source and a new exact cost statement are committed |
| A034 | 2026-07-26 | Prospective bounded replacement for pre-J-lens provenance stop | A033 is committed at exact source `23da31eb563e6386de980fd06c6dfca4454cc678`; the A032 pod was terminated and inventory is zero. The failed bundle is retained unchanged with three unopened private SAE/dense files, zero J-lens layers, and no public result | Bind the exact A033 runner source and the same immutable discovery/calibration manifests, private bundles, and probe plan. Authorize one replacement secure B200 at no more than `$5.98/hour`, 30-minute wall (`$2.99` maximum), 10-minute no-progress stop, no fallback, and a new provenance-bound output root. Preserve the A032 failed bundle and repeat no generation | The replacement repeats only deterministic derived readout computation under corrected provenance validation. Even charging both A032 and the full replacement cap leaves the cumulative study below the current `$100` soft gate and `$200` hard ceiling | Existing full execution authorization; fail-closed recovery and separate exact-cost gate |
| A035 | 2026-07-26 | Prospective G4 patch-execution semantics | G3 behavioral and mechanism results are frozen and reported; no 8B causal-patch continuation, safe positive-control result, patched HarmBench score, or patched downstream state exists | Bind G4 to prefill-only residual-post replacement at the assistant boundary, the exact nine intervention/control kinds, existing unpatched same-row baselines, deterministic cross-behavior matching and magnitude-matched randomization, fixed layer-2 and preceding-token negative controls, atomic private tensor replay, and a 20-pair single-token benign positive-control schema. Keep both placements and partitions separate. Calibration remains closed until one common discovery layer passes, and B2 remains closed until that layer reproduces on calibration | The reviewed scientific plan fixed the estimands and gates but not the runtime meanings of no-op, irrelevant-site, cross-behavior, batched prefill, or baseline reuse. Resolving those choices before any patch outcome prevents implementation-dependent causal selection and permits fail-closed local tests. Paid compute remains closed until the runner source, exact private positive-control hash, retained inputs, measured throughput basis, and a separate cost cap are committed | Existing full execution authorization; G4 stage gate, raw-outcome restrictions, and separate exact-cost gate |
| A036 | 2026-07-26 | Pre-compute provenance authorization gate | A035 is committed and pushed at exact source `ca23c2d8058ad6b8bd2853b8932104e64bcb84f6`; no G4 safe-control, throughput, target-patch, or patched-score outcome exists | Require every patch-run invocation to find a prospective run-specific authorization whose status, exact runner commit, partition, qualification/target mode, run ID, private-plan hash, and private plan's frozen A035 scientific-plan hash all match. A safe-only authorization must explicitly keep target generation closed, and cannot be reused for target generation | Receipt provenance after execution is insufficient if an unbound plan can start the model. This fail-closed gate makes the intended two-commit authorization pattern executable before any paid process loads the model and prevents a safe qualification from silently crossing into target outcomes | Existing full execution authorization; G4 stage gate and separate exact-cost policy. No paid compute is authorized by A036 alone |

An amendment entry must distinguish:

- clarification with no change to the estimand;
- pre-outcome design change;
- infrastructure correction;
- post-outcome exploratory analysis;
- new prospective protocol version.

The Git diff, not this table alone, is the authoritative record of what changed.
