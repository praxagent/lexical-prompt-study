# Plan amendments

The locked charter may change only through an entry added here before the
affected outcome is inspected.

| ID | Date | Status | Outcomes inspected? | Change | Rationale | Approval |
|---|---|---|---|---|---|---|
| A000 | 2026-07-25 | Baseline | OpenRouter exploratory outcomes only; no raw-checkpoint target outcomes | Lock Study Charter v1.0 | Establish an auditable starting point before implementation or paid compute | Human requested first plan commit |
| A001 | 2026-07-25 | Pre-outcome design change | No raw-checkpoint target outcomes | Validate HarmBench on JBB's 300 published human-majority comparisons using balanced accuracy and recall as gates; report precision without gating it; use the runtime affirmative-token probability for the paired primary score | Published HarmBench binary labels pass balanced accuracy (0.802) and recall (0.873) gates but have precision 0.653, so a predeclared 0.80 precision gate would reject the evaluator while discarding its useful paired continuous signal | Human authorized execution under the locked change-control process |
| A002 | 2026-07-25 | Pre-outcome implementation clarification and infrastructure correction | No raw-checkpoint target outcomes | Resolve the HarmBench continuous score as the exact one-token `Yes` versus `No` softmax; retain both turns for E02; persist figure byte-verification status; verify Git blob IDs for non-LFS artifacts; bind restricted generation files to trial receipts; reserve H200 KV-cache headroom with the previously validated 125 GiB-per-GPU sharding ceiling | Remove implementation ambiguity and make the frozen evaluator, visual contract, artifact inventory, crash-resume evidence, and paid topology fail closed without changing any estimand, split, threshold, arm, or prompt | Human authorized execution under the locked change-control and provenance policies |
| A003 | 2026-07-25 | Post-generation environment correction | Target generations complete but raw outcomes and evaluator scores not inspected; failed scorer and tokenizer smoke wrote 0 score receipts | Add the `sentencepiece` and `protobuf` runtime dependencies required to load and convert the frozen HarmBench Llama-2 tokenizer, then retry the unchanged scorer against the same generation receipts and output root | The first scorer invocation and first corrected tokenizer smoke failed during tokenizer construction before loading the evaluator or writing a score; this repairs the locked environment without changing prompts, outputs, evaluator weights, scoring code, estimands, or thresholds | Authorized operational recovery within the approved study protocol |
| A004 | 2026-07-25 | Post-score analysis implementation correction | All evaluator receipts exist and pass linkage checks, but raw outcomes were not inspected and no gate statistic was produced | Retain the generation receipt's `turn` field in the gate analyzer's joined provenance row, bind the gate artifact to the analyzer implementation hash and source commit, and cover the complete 20-behavior/160-receipt discovery topology in a regression test | The analyzer already selected turns 1 and 2 but omitted `turn` from the constructed row before filtering for the frozen turn-2 estimand; adding the missing field and provenance identities repairs execution without changing inputs, scores, bootstrap seed, thresholds, or estimand | Authorized fail-closed recovery within the approved study protocol |
| A005 | 2026-07-25 | Pre-mechanism operational freeze | Gate-1 and Gate-2 derived behavioral scores inspected; no mechanistic outputs and no raw generated text inspected | Resolve the primary J-lens statistic as vocabulary-z-scored mean refusal-probe logit minus mean compliance-probe logit at the turn-2 assistant boundary; freeze secondary generated-token indices 0, 1, 2, 4, 8, and 16, where index k includes generated tokens 0 through k and reads the residual at token k; evaluate every declared lens source layer under fitted J-lens, identity, and a deterministic per-layer dense Gaussian transport Frobenius-matched to the fitted Jacobian using base seed 20260725; run SAE discovery only on the discovery split at residual-post layer 50; rank positive full-minus-structural-sham paired activation deltas by paired mean divided by root-mean-square paired delta, subject to at least 10% full-arm prevalence; retain one primary and at most three secondary candidates; preserve decoder norm, prevalence, sparsity, and reconstruction diagnostics | Make the descriptive readout and discovery selection executable and fail closed before any mechanistic outcome exists, while retaining the charter's estimand, controls, split separation, narrow descriptive interpretation, and held-out causal gate | Authorized operationalization within the approved Gate-3 protocol |
| A006 | 2026-07-26 | Pre-mechanism CLI wiring correction | First bounded qualification invocation stopped at Python argument binding before reading the private plan, hashing artifacts, loading a model, or writing any output | Pass the artifact-manifest path to `run_mechanism_discovery` rather than the unrelated `run_behavior` call | The artifact hash gate was implemented and tested at function level, but its CLI argument was wired to the wrong call site; this corrects invocation only and does not change observations, transforms, statistics, controls, selection, or thresholds | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A007 | 2026-07-26 | Pre-readout qualification correction | The bounded retry hash-verified inputs, loaded the model, and wrote derived residual-state checkpoints, but stopped at the vocabulary-moment fixture before writing any J-lens, identity, random-control, SAE, or summary output; no activation values or raw generated text were inspected | Evaluate the direct full-vocabulary fixture with dtype-compatible RMSNorm/unembedding operations; separately gate the float32 analytic equivalence and the actual bfloat16 module path; bind captured-state checkpoints to source commit, plans, artifact hashes, model/tokenizer revisions, and run ID | The first fixture passed activation capture but supplied a float32 vector directly to a bfloat16 linear module. The correction makes the intended validation executable and prevents a checkpoint created by different code or artifacts from being resumed | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A008 | 2026-07-26 | Pre-receipt schema correction | Qualification v2 passed artifact, capture, and vocabulary-moment gates and began the first in-memory transport batch, but stopped before writing any layer receipt, SAE diagnostic, or summary; no margin, activation, or raw-text value was inspected | Add the already-produced `observation_id` and behavior `category` provenance fields to the strict mechanism receipt schema, remove the redundant misnamed `observation_sha256` alias, and use `observation_id` for resume-topology validation | The runner correctly included observation ID and category in every receipt candidate, while the strict schema omitted them and therefore failed closed before persistence. The correction aligns schema with the frozen figure contract without changing any computed value | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A009 | 2026-07-26 | Pre-SAE-selection diagnostic correction | Qualification v3 wrote all bounded J-lens/identity/random layer receipts, whose values were not inspected, then stopped before writing SAE diagnostics or a run summary because the real SAE contains at least one zero-norm decoder column | Retain zero decoder norms as valid diagnostic observations but make them categorically ineligible for candidate selection; continue to reject negative/non-finite norms and negative/non-finite ReLU activations | A zero decoder column is a valid dead/degenerate feature diagnostic, not evidence of corrupt weights, but it cannot define a steering direction. Explicitly retaining and excluding it is stricter and more informative than aborting the whole SAE map | Authorized fail-closed recovery within the approved Gate-3 protocol |
| A010 | 2026-07-26 | Prospective Gate-4 discovery-calibration freeze | Gate-1 and Gate-2 behavioral results and Gate-3 derived J-lens/SAE results inspected; no causal-intervention generation or held-out intervention outcome exists, and no raw generated text was manually inspected | Freeze primary SAE feature 10146; named secondary features 44802, 4057, and 3907 outside the primary family; matched controls 26453, 9105, and 40804 with the imperfect prevalence match disclosed; three seeded isotropic controls; direct normalized decoder-direction addition at residual-post layer 50 on the current final token at every decoding step; a discovery-only residual-norm-scaled rho ladder with deterministic sign/alpha eligibility and stopping rules; the 15-condition held-out attack topology; the 2-condition utility topology; paired base-behavior estimands, bootstrap gates, Holm control family, raw-outcome restrictions, exact receipts, and compute launch gate in `plans/gate4_intervention_v1.public.json` | Convert the discovery result into an executable, falsifiable causal protocol before any intervention outcome is produced, prevent confirmatory strength/sign selection, retain causal specificity controls, and separate a prompt-local causal claim from the stronger defense claim | Authorized prospectively by the user's approval to continue under the locked stage-gate and budget policy |
| A011 | 2026-07-26 | Pre-intervention numerical-semantics correction | No causal-intervention generation or held-out intervention outcome exists; a seeded local synthetic bfloat16 tensor fixture only | Define realized intervention norm as the effective float32 difference between post- and pre-residual values after the addition is cast to the model residual dtype; relax the requested-versus-realized relative tolerance from 0.1% to 5%, while retaining the separate hard 5% delta-to-pre-residual norm budget and forbidding clipping | The synthetic 8,192-dimensional bfloat16 fixture showed 0.47%-4.46% effective norm error across the frozen rho ladder because small coordinate updates are quantized when added to bfloat16 residuals. A 0.1% gate would reject the declared runtime representation rather than detect implementation drift. Recording the effective vector and allowing 5% is strict enough to catch material mismatch without pretending bfloat16 addition is exact | Authorized prospective numerical clarification under the locked Gate-4 change-control policy |
| A012 | 2026-07-26 | Pre-signed-output qualification diagnostics | Gate-4 qualification v1 wrote two zero-intervention receipts and stopped on the first signed prefill before producing a signed token or receipt; raw zero outputs and behavioral content were not inspected | Include requested norm, effective realized norm, relative error, and effective delta-to-pre-residual ratio in the fail-closed norm-mismatch exception | The 5% gate correctly stopped an under-resolved bfloat16 addition, but the exception omitted the derived numeric diagnostics needed to distinguish an implementation error from an unrepresentable low dose. Adding non-text norm diagnostics changes no vector, output, estimand, threshold, or selection rule | Authorized fail-closed diagnostic recovery within the prospective Gate-4 protocol |
| A013 | 2026-07-26 | Pre-signed-output bfloat16 tolerance calibration | Qualification v2 reproduced two zero-intervention receipts and stopped on the first signed prefill before producing a signed token or receipt; only derived norm diagnostics were inspected | Increase the requested-versus-effective realized norm tolerance from 5% to 10%; retain the effective post-minus-pre definition, exact per-step recording, forbidden clipping, and the independent 5% effective delta-to-pre-residual norm ceiling | On the actual B200/model/SAE path, the smallest frozen dose requested norm 0.0533935 and realized effective norm 0.0563576, a 5.551% mismatch, while the effective delta remained only 0.3324% of that event's pre-residual norm. A 10% representation tolerance admits this minor bfloat16 quantization difference without weakening the stricter residual-budget ceiling | Authorized prospective numeric calibration under the locked Gate-4 protocol before any signed output exists |
| A014 | 2026-07-26 | Post-outcome reporting addition | All Gate-4 discovery calibration receipts and derived scores inspected; selector returned no eligible alpha; held-out confirmatory outcomes remain unopened | Add a provenance-complete discovery calibration stop figure showing the four frozen signed half-span estimates and intervals, directional components, and runtime safety diagnostics from the immutable Gate-4 analysis artifact | Make the negative stage-gate result visually inspectable without changing a statistic, threshold, candidate, selection decision, or inference; the contracted held-out E06 and E07 figures remain absent because confirmatory intervention was not authorized by the frozen selector | Authorized reporting-only work after the prospectively frozen stop rule fired |
| A015 | 2026-07-26 | Post-replay reporting and frozen disposition | The four-arm feature-10146 replay aggregate was inspected; no Llama 3.1 8B or Qwen target outcome exists | Promote the hash-gated four-arm aggregate, add a provenance-complete E05b figure, report `0/20` base, `14/20` inert-length, `0/20` structural-sham, and `20/20` full prevalence, and enforce the prospectively frozen retirement of feature 10146 from confirmatory detector, breaker, and defense claims | The replay failed only the non-full prevalence ceiling because inert length activated the feature. Reporting the complete matrix corrects the prior full-versus-sham interpretation without changing the frozen replay rule | Authorized reporting and execution under the approved follow-up protocol |
| A016 | 2026-07-26 | Prospective follow-up placement-factor freeze | Gate-3/Gate-4 and four-arm replay outcomes inspected; no Llama 3.1 8B or Qwen target outcome exists | Replace the vague alternate-position robustness label with an explicit crossed scaffold-placement factor: `ep_before_request` and `ep_after_request`. Hold content and token budgets matched across orderings within each arm; produce separate behavioral, SAE, and J-lens results; prohibit pooled estimation and pooled candidate selection; require one common detector and any causal site to satisfy ordering-specific gates by a maximin/conjunction rule; keep base and ordinary-benign reference observations shared but never duplicate-count them | Placement may change both attack behavior and internal routing. Freezing the two orderings before generation prevents a favorable ordering from dominating a pooled result or being chosen after outcomes | Human explicitly requested both orderings as a controlled factor before follow-up outcomes |
| A017 | 2026-07-26 | Prospective placement-review repairs | A016 was committed and independently reviewed at high reasoning; no Llama 3.1 8B or Qwen target outcome exists | Add component-level token stability and rendering assertions; make discovery viability a screen and calibration a two-test Holm family; freeze maximin/threshold formulas and ties; align causal Holm testing and sensitivity; freeze detector missingness, fixed denominators, attainable counts, and fixed-panel interpretation; define the two-order claim boundary; and enumerate placement-inclusive work units | The independent successor review found five compact ambiguities that could otherwise mix placement with tokenization, permit outcome-dependent selection, or make error control and missingness inconsistent. The repairs add no arm, model, or outcome and preserve the user's exact two-order, no-pooling design | High-reasoning placement review `9c1c7f7cb03337ea8c16dee4aa2b9a20e0fea5285b14024bbf37c72a7e1357f1`; all B01-B05 accepted, I01-I03 accepted |
| A018 | 2026-07-26 | Prospective G1 infrastructure substitution | The exact A40 request failed with `could not find any pods with required specifications`; provider inventory confirmed no task-owned pod and no target or qualification outcome exists | Substitute exactly one secure RTX A6000 48 GB for G1 at the live `$0.53/hour` rate, retain the 90-minute wall limit and 20-minute no-progress timeout, and revise the maximum qualification compute cost from `$0.66` to `$0.795`; automatic fallback remains forbidden | The A6000 is the least-cost currently reported secure 48 GB substitute with non-null stock. G1 measures engineering compatibility, throughput, and memory on a synthetic-safe request, so the substitution changes the infrastructure receipt and downstream cost estimate but no scientific arm, prompt, outcome, selection, threshold, or claim | Existing full execution authorization; fail-closed cost-statement clause in the frozen G1 plan |
| A019 | 2026-07-26 | Prospective G1 region-compatible infrastructure substitution | The A018 RTX A6000 request also failed with `could not find any pods with required specifications`; post-failure inventory again confirmed no task-owned pod and no target or qualification outcome exists | Substitute exactly one secure B200 for G1 at the live `$5.89/hour` rate, shorten the wall limit from 90 to 30 minutes and the no-progress timeout from 20 to 10 minutes, and revise the maximum qualification compute cost to `$2.945`; automatic fallback remains forbidden | Global stock indicators did not reflect allocatability with the retained `US-CA-2` volume, while this exact volume previously admitted a secure B200. The shorter cap bounds the premium-device exposure while preserving the synthetic engineering qualification and persistent setup checkpoint. This changes no scientific arm, prompt, outcome, selection, threshold, or claim | Existing full execution authorization; fail-closed cost-statement clause in the frozen G1 plan |
| A020 | 2026-07-26 | Pre-generation tokenizer-boundary implementation correction | Pinned artifacts were downloaded and hash-verified, but the synthetic placement preflight stopped before model loading or generation because the first renderer could not recover the standalone component token subsequences in either ordering; no qualification or target outcome exists | Wrap each immutable request/scaffold block with the same frozen Llama reserved boundary tokens 0 and 1 and separate the two blocks with reserved boundary token 2; retain the same component bytes and all existing equality, unique-offset, total-token, context, and assistant-suffix assertions | The real pinned tokenizer merges ordinary textual delimiters with adjacent content, while reserved special tokens create explicit tokenizer boundaries. A synthetic tokenizer check established exactly one identical request and scaffold subsequence and equal 75-token prompts in both orderings before this correction was frozen | Authorized fail-closed implementation recovery under G0/G1; no model state or outcome existed |
| A021 | 2026-07-26 | Synthetic qualification checkpoint-key correction | A020 passed render validation, loaded the pinned model, and completed the first synthetic-safe generation in memory, then stopped before persistence because the private checkpoint store correctly rejected underscores in the derived stage key; no raw generation was persisted or inspected, no mechanism output exists, and no target content was used | Normalize the two already-frozen placement labels to hyphenated private stage keys (`generation-ep-before-request`, `generation-ep-after-request`) and add an exact uniqueness/allowlist regression test; change no render, token, model, generation, mechanism, statistic, or public receipt field | Placement labels are valid scientific identifiers but were interpolated directly into a stricter filesystem checkpoint namespace. Deterministic key normalization restores the intended atomic resume path without changing an outcome or estimand | Authorized fail-closed operational recovery within synthetic G1 |
| A022 | 2026-07-26 | Pre-target decoding and capture implementation freeze | Synthetic G1 passed; no harmful/benign follow-up target generation or target-model state exists | Freeze each follow-up observation as one user turn followed by deterministic assistant generation with no system prompt, seed 0, `do_sample=false`, `use_cache=true`, an 8,192-token context ceiling, and at most 1,024 generated tokens; generate shared base/ordinary-benign references once; cross the three structured harmful arms and structured-benign arm with both placement orderings; capture every pinned J-lens source layer plus SAE layer 19 at the assistant boundary before the first generated token; checkpoint raw generations, reconstructive token IDs, and bfloat16 states privately after every observation | The placement review froze matched single-turn orderings but the executable decoding ceiling and capture ledger were not yet machine-bound in the public JSON. This resolves those implementation degrees of freedom before any target outcome and makes the 860-unit work ledger exact | Existing full execution authorization and G0/G1 stage-gate policy |
| A023 | 2026-07-26 | Pre-target assistant-suffix hash correction | Synthetic G1 passed under prompts that remain unchanged; no harmful/benign follow-up target generation or target-model state exists. A local target-runner fixture showed that a fixed last-eight-token hash can include the tail of the second component when the common epilogue is short | Define the frozen assistant-boundary suffix as every prompt token strictly after the final recovered request/scaffold component offset, including the common block terminator, epilogue, and chat-template assistant boundary; require that suffix to be non-empty and identical across orderings | Offset-relative hashing measures the intended invariant directly and cannot accidentally depend on which component is second. This changes no rendered byte, token, generation, state, estimand, or G1 engineering conclusion | Authorized fail-closed pre-target implementation recovery under A017's exact-offset requirement |
| A024 | 2026-07-26 | Prospective G2 discovery compute statement | Synthetic G1 receipts and timings inspected; real-tokenizer discovery/calibration render preflights passed; no harmful/benign target generation or target-model state exists | Authorize only the 140-observation G2 discovery generation/capture run on exactly one secure B200 at `$5.89/hour`, using the slower G1 placement rate of 21.410 generated tokens/second, a 150-minute wall limit (`$14.725` maximum), 15-minute no-progress timeout, and no automatic fallback | At 1,024 tokens for every trial, the conservative generation estimate is 6,695.9 seconds (`$10.96`), leaving roughly 30 minutes for exact verification, model load, state capture, receipts, and teardown. Calibration remains gated on the discovery behavioral screen and a separate cost statement | Existing full execution authorization; measured-throughput and per-run cost gates in the frozen plan |
| A025 | 2026-07-26 | Prospective G2 live-rate reconciliation | Immediately before launch, the provider's read-only inventory reported the secure B200 rate had moved from `$5.89/hour` to `$5.98/hour`; no G2 pod, target generation, or target-model state existed | Retain exactly one secure B200, the 140-observation discovery topology, 150-minute wall limit, 15-minute no-progress timeout, and no-fallback rule; update only the live rate and corresponding maximum compute from `$14.725` to `$14.95` | The live-rate change is a 1.53% infrastructure price movement and changes no prompt, placement, model, decoding rule, state capture, estimand, selection rule, or scientific claim | Existing full execution authorization; fail-closed exact-cost gate in A024 |
| A026 | 2026-07-26 | Pre-score follow-up evaluator adapter and bootstrap stream freeze | G2 generation/capture completed and its receipt/private bundle passed integrity validation; no raw generation or HarmBench score was inspected and no behavioral contrast was computed | Permit the pinned HarmBench scorer to resolve the follow-up receipt's `restricted_artifact_path` and partition-indexed private behavior rows; freeze discovery bootstrap base seed 20260726, use NumPy `default_rng(seed + order_index)` for the two separately reported ordering strata, and `seed + 2` for the descriptive paired placement interaction | The follow-up receipt and private-plan schemas intentionally differ from v1, while the evaluator model, exact Yes/No scoring rule, prompts, generated bytes, 10,000-replicate percentile bootstrap, thresholds, and no-pooling rule are unchanged. Freezing deterministic streams before scoring removes an implementation degree of freedom | Existing full execution authorization; pre-analysis fail-closed implementation recovery |
| A027 | 2026-07-26 | Pre-score G2 provenance binding and bounded scorer compute | G2 generation/capture and a no-model resume audit completed; 140 receipts, 140 restricted artifacts, and 140 state bundles were retrieved with matching deterministic bundle hash. Raw outcomes and HarmBench scores remain unopened | Bind the exact generation source/public/private plans, run ID, render preflight, bundle hash, and 140/140/140 topology in the public plan. Authorize only pinned HarmBench scoring of the 140 receipts on exactly one secure B200 at no more than `$5.98/hour`, with a 20-minute wall (`$1.9934` maximum), 10-minute no-progress stop, and no fallback | The same evaluator scored 180 prior intervention receipts in 49.28 seconds; linear scaling estimates 38.33 seconds for 140, leaving 19.36 minutes for allocation, exact verification, model load, receipt checks, retrieval, and teardown. Exact input binding prevents scoring a substituted discovery bundle | Existing full execution authorization; receipt-first stage gates and exact per-run cost policy |
| A028 | 2026-07-26 | Zero-score follow-up classifier-text key correction | The first A027 scorer invocation stopped before evaluator construction/load with `KeyError: behavior` and wrote 0/140 score receipts. Generation outputs and raw text were not inspected | Resolve classifier target text from the v1 private-row key `behavior` or the follow-up private-row key `request`, requiring one non-empty string; change no classifier prompt template, behavior bytes, generation bytes, evaluator, label tokens, scoring rule, threshold, bootstrap, or receipt | The follow-up plan intentionally labels the immutable behavior string `request`; the generic scorer adapter handled its partition and artifact-path schema but not this final key rename. A synthetic regression test covers both schemas before retry | Authorized fail-closed same-pod operational recovery within A027's original wall/cost cap |
| A029 | 2026-07-26 | Prospective G2 calibration statistics and generation cap | The receipt-backed discovery screen passed separately in both placement orderings and its public result was committed; no calibration generation, state, or score exists | Freeze 65,536 two-sided paired sign-flip draws with plus-one p-values at seeds 20260729 and 20260730 for before/after placement, Holm family alpha 0.05 with placement-order tie break, and a descriptive 10,000-bootstrap interaction at seed 20260731. Authorize only 140 calibration generation/capture observations on one secure B200 at no more than `$5.98/hour`, 45-minute wall (`$4.485` maximum), 15-minute no-progress stop, and no fallback; scoring remains closed until the completed bundle is hash-bound | Discovery generation/capture completed in 569.31 seconds. The 45-minute cap leaves 35.51 minutes for allocation, exact plan/artifact verification, model load, receipt validation, retrieval, and teardown while keeping the formal test implementation fixed before calibration outcomes | Existing full execution authorization; G2 stage gate and exact per-run cost policy |
| A030 | 2026-07-26 | Pre-score calibration provenance binding and scorer cap | Calibration generation/capture completed in 538.24 seconds; its 140 receipts, 140 restricted artifacts, and 140 state bundles passed a no-model resume audit and local/remote bundle-hash match. Raw outcomes and HarmBench scores remain unopened | Bind exact calibration generation source/public/private plans, run ID, render preflight, bundle hash, and topology. Authorize only pinned HarmBench scoring of 140 receipts on one secure B200 at no more than `$5.98/hour`, 20-minute wall (`$1.9934` maximum), 10-minute no-progress stop, and no fallback | Discovery scoring of the same receipt count completed in 37.27 seconds, leaving 19.38 minutes for exact verification, evaluator load, score validation, retrieval, and teardown. The analyzer must reject any calibration receipt outside the frozen binding | Existing full execution authorization; formal calibration gate and exact per-run cost policy |
| A031 | 2026-07-26 | Prospective G3 SAE/J-lens readout freeze | G2 behavioral discovery and formal calibration results were inspected separately for both placements; the 280 assistant-boundary state bundles exist, but no Llama 3.1 SAE activation, candidate identity, subspace, dense comparator, J-lens margin, identity margin, or random-transport margin has been generated or inspected | Freeze common-feature eligibility in both placements, maximin individual ranking, one single feature, one at-most-eight-feature subspace with inverse-worst-order-RMS positive L2-normalized weights, calibration selection between only those candidates, a secondary equal-order-weight dense mean-difference projection, exact inherited refusal/compliance token IDs and semantic hashes, all 31 lens source layers, fitted/identity/Frobenius-matched Gaussian transports, deterministic 10,000-bootstrap streams, private complete activation/per-observation receipts, and separate discovery/calibration × before/after reporting with no pooled placement estimate. Defer the common detector threshold until the ordinary-benign and structured-benign utility-calibration negatives exist | The prior protocol fixed the maximin selector but not how the subspace, probe replay, bootstrap streams, or incomplete-negative threshold boundary were constructed. This amendment removes those remaining degrees of freedom before mechanism readouts and prevents an incomplete harmful-only calibration matrix from being mislabeled as a deployable detector | Existing full execution authorization; G3 stage gate and raw-outcome restrictions. Paid readout compute remains closed until exact A031 source, input manifests, local tests, throughput basis, and a separate cost cap are committed |
| A032 | 2026-07-26 | Prospective G3 exact-input and compute authorization | A031 is committed and pushed at exact source `655fa9b69b185cfcbad5ce51fb027909c1d73d18`; all local tests pass; the discovery and calibration receipt/state manifests were validated without opening raw prompts, generations, or reconstructive token IDs; no G3 mechanism value exists | Bind the exact A031 runner source, both 140-state input manifests and private bundles, the inherited probe-plan hash, and the expected public/private receipt topology. Authorize only one secure B200 at no more than `$5.98/hour`, with a 30-minute wall (`$2.99` maximum), 10-minute no-progress stop, atomic resume, and no fallback | The synthetic-safe B200 qualification completed two observations and exercised model, SAE, J-lens, identity, random-control, and patch paths in 7.3718 seconds. Conservative linear scaling to 280 captured states is 1,032.05 seconds, leaving 12.80 minutes for exact verification, model/artifact load, vectorized analysis, receipt validation, retrieval, and teardown. Generation is not repeated | Existing full execution authorization; G3 exact-cost gate, current `$100` cumulative soft gate, and `$200` cumulative hard ceiling |
| A033 | 2026-07-26 | Post-SAE/pre-J-lens local-snapshot provenance correction | The exact A032 runner validated all 280 input states and wrote private mode-0600 SAE activation, unopened discovery diagnostics, and dense-direction checkpoints, then stopped before model/J-lens loading because a tokenizer loaded offline from the exact local Hugging Face snapshot had no `_commit_hash`. No feature identity/value, dense value, J-lens value, raw prompt, generation, or reconstructive token ID was inspected; no public result exists | Accept a missing loaded-object `_commit_hash` only when the canonical local snapshot directory basename exactly equals the pinned model revision; continue to reject any non-null mismatched object hash. Retain exact tokenizer semantic-hash checks, model topology checks, artifact hashes, input manifests, and all scientific rules | Hugging Face local-only loading does not reliably populate the optional object metadata even when the caller supplies the exact immutable snapshot directory. Binding the canonical path to the pinned revision plus semantic/topology checks restores the intended fail-closed provenance gate without changing or inspecting an estimand | Existing full execution authorization; fail-closed implementation recovery. Replacement compute remains closed until this source and a new exact cost statement are committed |
| A034 | 2026-07-26 | Prospective bounded replacement for pre-J-lens provenance stop | A033 is committed at exact source `23da31eb563e6386de980fd06c6dfca4454cc678`; the A032 pod was terminated and inventory is zero. The failed bundle is retained unchanged with three unopened private SAE/dense files, zero J-lens layers, and no public result | Bind the exact A033 runner source and the same immutable discovery/calibration manifests, private bundles, and probe plan. Authorize one replacement secure B200 at no more than `$5.98/hour`, 30-minute wall (`$2.99` maximum), 10-minute no-progress stop, no fallback, and a new provenance-bound output root. Preserve the A032 failed bundle and repeat no generation | The replacement repeats only deterministic derived readout computation under corrected provenance validation. Even charging both A032 and the full replacement cap leaves the cumulative study below the current `$100` soft gate and `$200` hard ceiling | Existing full execution authorization; fail-closed recovery and separate exact-cost gate |
| A035 | 2026-07-26 | Prospective G4 patch-execution semantics | G3 behavioral and mechanism results are frozen and reported; no 8B causal-patch continuation, safe positive-control result, patched HarmBench score, or patched downstream state exists | Bind G4 to prefill-only residual-post replacement at the assistant boundary, the exact nine intervention/control kinds, existing unpatched same-row baselines, deterministic cross-behavior matching and magnitude-matched randomization, fixed layer-2 and preceding-token negative controls, atomic private tensor replay, and a 20-pair single-token benign positive-control schema. Keep both placements and partitions separate. Calibration remains closed until one common discovery layer passes, and B2 remains closed until that layer reproduces on calibration | The reviewed scientific plan fixed the estimands and gates but not the runtime meanings of no-op, irrelevant-site, cross-behavior, batched prefill, or baseline reuse. Resolving those choices before any patch outcome prevents implementation-dependent causal selection and permits fail-closed local tests. Paid compute remains closed until the runner source, exact private positive-control hash, retained inputs, measured throughput basis, and a separate cost cap are committed | Existing full execution authorization; G4 stage gate, raw-outcome restrictions, and separate exact-cost gate |
| A036 | 2026-07-26 | Pre-compute provenance authorization gate | A035 is committed and pushed at exact source `ca23c2d8058ad6b8bd2853b8932104e64bcb84f6`; no G4 safe-control, throughput, target-patch, or patched-score outcome exists | Require every patch-run invocation to find a prospective run-specific authorization whose status, exact runner commit, partition, qualification/target mode, run ID, private-plan hash, and private plan's frozen A035 scientific-plan hash all match. A safe-only authorization must explicitly keep target generation closed, and cannot be reused for target generation | Receipt provenance after execution is insufficient if an unbound plan can start the model. This fail-closed gate makes the intended two-commit authorization pattern executable before any paid process loads the model and prevents a safe qualification from silently crossing into target outcomes | Existing full execution authorization; G4 stage gate and separate exact-cost policy. No paid compute is authorized by A036 alone |
| A037 | 2026-07-26 | Prospective safe-only G4 qualification authorization | A036 is committed and pushed at exact source `3ae84aec497feb07f2bb139f29f63f3623852749`; read-only provider inventory is empty; no G4 safe-control, throughput, target-patch, or patched-score outcome exists | Bind the exact A036 runner, A035 scientific-plan hash, private safe-plan hash, model revision, retained volume, run ID, and output root. Authorize only 20 benign single-token patch pairs plus one batch of 20 benign repetition prompts on exactly one secure B200 at the live `$5.89/hour` rate, with a 20-minute wall (`$1.9634` maximum), 10-minute no-progress stop, no fallback, and zero target-patch receipts. Target generation remains explicitly closed | The safe positive control tests whether the hook can move a known next-token margin, while the benign batch measures the real model/hook generation throughput and peak memory needed for a separate target cost gate. Separating this from target authorization prevents engineering calibration from producing or selecting on target outcomes | Existing full execution authorization; measured-throughput gate, `$100` cumulative soft gate, `$200` hard ceiling, and exact per-run cost policy |
| A038 | 2026-07-26 | Prospective layer-specific instrument-strength revision | A037 safe-only results were inspected: identity and no-op controls were exact at all nine layers; donor replacement passed the frozen strength gate at layers 16, 20, 24, 28, and 31 but failed at 0, 4, 8, and 12. A037 produced zero target patch trials and no throughput generation; no target patch outcome or patched score exists, and the pod is terminated | Interpret the predeclared positive control as a layer-specific instrument-strength screen: retain exactly the five passing layers for prospective target discovery, report and exclude the four failing layers, and invalidate the causal arm only if no layer passes or any retained layer fails identity/no-op. Bind the immutable A037 public result hash and prohibit target-outcome-based layer selection. Permit future runs to reuse that exact public safe-control result rather than rerun it | Requiring every coarse layer to causally transfer an arbitrary safe next-token answer conflated failure of a particular layer-level manipulation instrument with failure of the hook class. The exact controls show the hook is inert when requested and becomes strongly effective in later layers. Restricting causal claims to independently validated instrument-strong layers is stricter than estimating effects where the manipulation could not work, while preserving a transparent record of the post-safe/pre-target revision | Existing full execution authorization and change-control policy; this is an engineering-instrument calibration revision made before any target patch outcome. Paid throughput and target compute remain closed pending exact source and cost amendments |
| A039 | 2026-07-26 | Pre-throughput run-specific authorization lookup | A038 is committed and pushed at exact source `8419f1f`; the A037 pod is terminated, no pod exists, and no target patch or throughput outcome exists | Resolve qualification authorizations by an exact unique run ID among G4 qualification entries, accepting only the two explicit safe-only or throughput-only statuses. Retain the separate fixed discovery/calibration target authorization names. Continue to require exact runner, partition, mode, private-input hash, run ID, and target-closed flags | The retained A037 authorization is immutable evidence and cannot be overwritten to authorize the distinct post-A038 throughput retry. Exact run-ID lookup permits a new separately costed authorization while preventing either qualification from being confused with target generation | Existing full execution authorization; G4 stage gate and separate exact-cost policy. No paid compute is authorized by A039 alone |
| A040 | 2026-07-26 | Prospective G4 benign-throughput-only authorization | A039 is committed and pushed at exact source `9496d4fdeb1c77963702979751aae97d6979ff63`; the A037 public safe-control receipt is immutable and promoted; provider inventory is empty; no throughput or target patch outcome exists | Bind exact A039 source, A038 scientific-plan hash, original private safe-input plan/hash, promoted A037 safe-result hash, model revision, retained volume, run ID, and output root. Authorize only one batch of 20 benign repetition prompts under a no-op layer-0 hook on exactly one secure B200 at no more than `$5.89/hour`, with a 20-minute wall (`$1.9634` maximum), 10-minute no-progress stop, no fallback, no safe-control rerun, and zero target-patch receipts | This supplies the missing measured batch decode throughput and peak-memory basis for a separate target cost gate without repeating the already-frozen instrument calibration or exposing target outcomes. Even charging A037 and A040 at their full caps leaves the conservative cumulative study spend below the `$100` soft gate | Existing full execution authorization; measured-throughput gate, `$100` soft gate, `$200` hard ceiling, and exact per-run cost policy |
| A041 | 2026-07-26 | Pre-model A040 source-hash correction | The sole A040 pod reached SSH, but checkout stopped because the authorization expanded abbreviated commit `9496d4f` to a nonexistent 40-hex value. No runner started, model loaded, safe output generated, or target outcome exists | Correct only the A040 runner source binding to the actual immutable A039 commit `9496d4fdeb1c77963702979751aae97d6979ff63`; retain the same pod, wall deadline, run ID, plans, safe result, model, scope, and zero-target rule | A Git abbreviation is not sufficient provenance and must be resolved by the repository, not manually expanded. The failed checkout demonstrates the fail-closed gate; the correction changes no code, input, estimand, outcome, or cost cap | Authorized operational provenance recovery within A040's unchanged exact-pod cost window |
| A042 | 2026-07-26 | Post-throughput deterministic projection correction | A040/A041 benign throughput completed: 20,480 aggregate token lanes in 24.4056 seconds, 839.151 aggregate token lanes/s, 19,270,214,656 peak GPU bytes, and zero target trials. The immutable receipt's runtime projection was inspected; no target patch outcome exists and the pod is terminated | Preserve and publish the immutable throughput receipt, but correct its deterministic target projection from the pre-A038 nine-layer grid (3,240 trials; 3,317,760 maximum tokens; 3,953.7 seconds) to the already-frozen five instrument-passing layers (1,800 trials; 1,843,200 maximum tokens; 2,196.51 seconds). Update future runner receipts to use the five-layer grid | The measured rate and memory are valid; only the multiplication used the superseded coarse-layer count. The corrected arithmetic follows directly from the prospective A038 target topology and changes no measurement, layer selection, prompt, generation, estimand, or target outcome | Existing full execution authorization; receipt correction and measured-throughput cost gate. Target compute remains closed until this source/result and a separate exact cap are committed |
| A043 | 2026-07-26 | Prospective G4 target discovery authorization | A042 is committed/pushed at exact source `24f01796932151ed634991cd033d61b5fd4d3344`; the immutable safe-control and throughput receipts are promoted and bound; no target patch outcome exists and provider inventory is empty | Authorize exactly 1,800 discovery patch continuations: 2 placement orderings × 5 instrument-passing layers × 9 frozen intervention/control conditions × 20 behaviors, each capped at 1,024 tokens. Bind exact source/scientific/private/safe/throughput/input-manifest/model/volume hashes. Use one secure B200 at no more than `$5.89/hour`, a 60-minute wall (`$5.89` maximum), 10-minute no-progress stop, atomic per-trial resume, no fallback, and no placement pooling | Measured benign throughput projects 2,196.51 generation seconds; the cap leaves 23.39 minutes for exact verification, model/input load, state patch preparation, 1,800 atomic receipt/replay writes, retrieval, and teardown. Charging prior un-ingested qualifications and this full cap gives a conservative cumulative `$93.3079`, below the `$100` soft gate and `$200` hard ceiling | Existing full execution authorization; G4 discovery gate, raw-outcome restrictions, measured-throughput policy, and exact per-run cost gate. Scoring and calibration remain closed |
| A044 | 2026-07-26 | Post-720/pre-layer-31 derived-state and immutable-resume correction | A043 completed 36/90 batches and exact 720 receipt/artifact/replay triples, then stopped before the first layer-31 batch because retained G2 bundles contain residual-post keys 0–30 only. Raw generations and behavioral scores remain unopened; the task pod is terminated; the exact partial bundle is retrieved and hash-bound | Preserve the 720 completed units byte-for-byte. For missing layer 31 only, capture assistant-boundary residual-post states from the same frozen full/sham prompt token IDs and pinned model in four batched forward passes (placement × arm), checkpoint each tensor bundle mode 0600 with exact source/plan/prompt hashes, and use its bundle hash as donor-state provenance. Permit resume only from the exact predecessor source/plan/run and 720-unit partial manifest; accept predecessor receipts without rewriting them, then generate only missing batches | Layer 31 passed the independent safe instrument-strength gate, but its donor states were not in the earlier J-lens capture grid. A derived forward-state checkpoint measures the same pre-generation site without generating or selecting on a target outcome. Exact mixed-provenance resume is scientifically preferable to discarding 720 immutable completed units or silently excluding the strongest instrument-valid layer | Existing full execution authorization; fail-closed G4 implementation recovery. Replacement compute remains closed until this source, partial manifest, derived-state schema, local tests, and a separate cost cap are committed |
| A045 | 2026-07-26 | Prospective G4 partial-resume compute authorization | A044 is committed/pushed at exact source `270b2818a371aeba90aa91b2892689973fba44b1`; exact 720-unit partial provenance is frozen; raw generations and behavioral scores remain unopened; no task-owned pod exists | Authorize the same run ID and output root on exactly one secure B200 for at most 45 minutes at no more than `$5.89/hour` (`$4.4175` maximum). Verify and preserve the 720 predecessor triples, derive exactly four missing layer-31 state checkpoints, generate only the remaining 1,080 trials, require 1,800 final triples, prohibit overwrite/pooling/fallback, and keep raw prompts, generations, token IDs, and states private | The shortened replacement wall is sufficient for the measured remaining generation work plus model/state setup while charging the entire failed A043 cap conservatively. Its `$97.7254` post-run ceiling retains a separately costed 20-minute scoring reserve below the `$100` soft gate | Existing full execution authorization; immutable-resume policy, measured-throughput gate, `$100` soft gate, `$200` hard ceiling, and exact per-run cost policy |
| A046 | 2026-07-26 | Pre-launch G4 provider-rate correction | A045 is committed/pushed at `955e5f3`; a fresh provider inventory showed only unrelated pod `x4kh3wrrnycfzw`; the secure B200 catalog rate was `$5.98/hour`, exceeding A045's `$5.89/hour` cap; no task pod was created and no outcome was generated or inspected | Raise only the replacement live-rate ceiling to `$5.98/hour`, its 45-minute cap to `$4.485`, and the separately reserved 20-minute scorer allowance to `$1.9934`; preserve source, run ID/root, topology, inputs, partial manifest, trial counts, time limits, no-progress stop, no fallback, and all raw-outcome restrictions | Fail closed on provider-rate drift while retaining the exact scientific protocol. Charging both revised caps gives a conservative `$99.7863` ceiling, still below the `$100` soft gate and `$200` hard ceiling | Existing full execution authorization; pre-launch rate verification and prospective exact-cost policy |
| A047 | 2026-07-26 | Post-generation G4 immutable result binding | A046 completed all 90 batches and exact 1,800 receipt/artifact/replay triples, including 720 byte-preserved predecessor trials and 1,080 new trials; the four derived layer-31 state checkpoints exist; raw generations and behavioral scores remain unopened; task pod `js87bsj0oj8nsg` is terminated | Bind the exact runner source/A046 plan, summary and derived-state-index hashes, canonical 5,409-file non-log private manifest, topology/counts, runtime, pod/rate/teardown receipt, and unopened-score boundary. Mark generation complete and keep scoring closed until a separate source/input/cost authorization is committed | Establish an immutable handoff from target generation to blinded evaluation and prevent a partial, drifted, or regenerated bundle from entering causal analysis | Existing full execution authorization; post-generation provenance promotion under the locked raw-outcome and stage-gate policies |
| A048 | 2026-07-26 | Prospective G4 complete-bundle scoring authorization | A047 is committed/pushed at exact source `55a1be9708e46e57b8ac64901502d7e11e48a742`; the complete 1,800-trial manifest and scorer implementation are hash-bound; no task pod exists and no patch score has been generated or inspected | Authorize only the pinned HarmBench classifier pass for all 1,800 immutable patch receipts on exactly one secure B200 at no more than `$5.98/hour`, with a 20-minute wall (`$1.9934` maximum), 10-minute no-progress stop, atomic resume, no generation, and no fallback. Bind the original 20-behavior private discovery plan, evaluator revision, scoring-code hash, generation source/plan/manifest/summary, output root, and exact score count | The same pinned evaluator scored 180 receipts in 49.28 seconds, projecting 492.8 seconds and leaving 11.79 minutes for verification, load, writes, retrieval, and teardown. Charging the full scoring cap yields a conservative cumulative `$99.7863`, below the `$100` soft gate | Existing full execution authorization; blinded evaluation stage gate, `$100` soft gate, `$200` hard ceiling, and exact per-run cost policy |
| A049 | 2026-07-26 | Post-score/pre-analysis G4 immutable score binding | A048 completed exact 1,800/1,800 HarmBench receipts; the no-model completed resume validated every generation-receipt/scorer/evaluator linkage and wrote zero; a schema audit verified finite paired probabilities; no score value, classification, or raw classifier input was inspected; task pod `q9nqmpij7w1r82` is terminated | Bind the exact scoring source/A048 plan, score summary, canonical 1,801-file private manifest, evaluator/scorer identities, invocations, pod/rate/teardown receipt, and unopened-analysis boundary. Permit only the already-frozen local coarse-patch analysis over this bundle and the immutable G2 discovery baselines | Establish a blinded, immutable scoring-to-analysis handoff before any causal effect estimate or layer eligibility result is computed | Existing full execution authorization; post-score provenance promotion and frozen G4 discovery analysis gate |
| A050 | 2026-07-26 | Post-analysis G4 result binding and reporting | The frozen local analysis inspected all 1,800 patch scores and returned no eligible layer. Neither placement produced a restoring patch with mean at most `-0.1`, upper 95% bound below zero, and required concordance; neither produced a reciprocal patch with mean at least `0.1`, lower 95% bound above zero, and required concordance. No placement was pooled, no raw prompt or generation was inspected, and no calibration or confirmatory patch outcome exists | Bind the exact A049 input-plan hash, analysis source and implementation, public/private result hashes, complete counts, empty eligible-layer set, null selected layer, and closed calibration state. Add a provenance-complete E12 forest/gate figure and update the report without changing any statistic or eligibility rule | Make the negative causal-localization outcome auditable and prevent an ineligible discovery layer from advancing. The stop applies to the tested coarse residual-post replacement sites; it does not establish that no finer causal circuit exists | Existing full execution authorization; post-analysis reporting under the frozen G4 stop rule |
| A051 | 2026-07-26 | New prospective 8B scaffold-by-request-content and injection-size protocol | The completed 8B follow-up selected feature `6779` after discovery and candidate calibration, observed assistant-boundary activation of `0/20` in harmful base and inert-length arms, `20/20` in full arms, and ordering-dependent structural-sham activation, then stopped its coarse causal arm with no eligible layer. No neutral-carrier, ordinary-benign-with-scaffold, scaffold-only, size-dose, new threshold-calibration, fine feature/component intervention, small-Qwen transfer, or shadow outcome exists; the original harmful confirmation panel remains unopened | Freeze a partially crossed request class × scaffold content × placement × injection-size design. At every nonzero size and placement, require exact realized injected-token matching across full, structural-sham, and inert controls; retain an unpadded shared no-scaffold reference; resolve `25%`, `50%`, `75%`, and `100%` doses only at predeclared complete restricted block boundaries; never pool placement or size. Decompose length, structure, and full-content effects; test harmful-minus-benign and harmful-minus-neutral full-content interactions; keep feature `6779` and the existing eight-feature subspace frozen; require a genuinely new threshold-calibration panel and one-shot unopened confirmation; firewall exploratory feature interpretation; preserve SAE reconstruction error in later feature edits; benchmark cheap lexical/structural detectors; keep Qwen 397B and automatic breaker deployment closed | The current four harmful arms establish neither scaffold sufficiency nor a harmful-content interaction, and strict-positive prevalence is not an operational threshold. Explicit request-content and injection-size controls distinguish a fixed-text/amount marker from a content interaction while preserving the original placement finding. The staged threshold, causal, transfer, and shadow gates prevent a correlational coordinate from becoming a defense claim without specificity, causal, utility, and latency evidence | Author's explicit full-program approval on 2026-07-26, including the added requirement to control injection size. A051 authorizes protocol implementation and local validation only; paid compute still requires an exact source/rate/wall-time/spend amendment under the `$100` soft gate and `$200` hard ceiling |
| A052 | 2026-07-26 | Prospective adversarial-review repair and staged factorial freeze | A051 was committed at exact source `94607c68e6410d9daa118e7318195c152ff6394b` and underwent one result-free high-reasoning director review. The valid private review has input SHA-256 `c27efd2464f5a7e199ff1f2c0a4cb6e9642b4eb311e5ff66a3339e273bb1daac`, review SHA-256 `f341710b34c7ce332c7069aa62bd2a99953bfb6f01863216b6036a9a7d979d7b`, verdict `NOT READY TO FREEZE`, and cost `$1.331955`. No new private factorial material, target outcome, threshold, or paid GPU exists | Accept all five blockers. Narrow the claim to the exact frozen full/inert/sham materials and fixed request panels; rename generic length/structure effects as inert-injection and sham-over-inert increments; permit mixed contributions; freeze a `0.05` feature-unit practical margin, simultaneous fixed-panel stability intervals, a positive/mixed/invalid decision table, prompt-family independence, missingness/judge rules, exact duplicate-dose merging, and an executable canary/serialization/SAE-reconstruction gate. Reduce the canonical run to core readouts, defer all-layer trajectories and maps, separate detector confirmation into a later powered protocol with cheap baselines, relabel Qwen as joint-shift replication, make shadow mode non-enforcing, and bind a staged 8-canary/422-canonical/540-additional-dose bill of materials | The repair preserves the user's exact injection-size matching and before/after placement requirements while preventing one inert string or sham from supporting generic construct claims. Separating the low-FPR detector study avoids underpowered false-positive claims and selective truncation. The worst-case prior-throughput projection for canonical plus dose is `$76.42`, which would put the conservative infrastructure ceiling near `$163.35` before scoring and future volume: below the `$200` hard ceiling but above the `$100` soft gate, so a later exact staged cost amendment and renewed explicit authorization remain mandatory | User approved the full follow-up program and specifically required EP-injection size controls. A052 authorizes local implementation only; it does not authorize target generation, threshold fitting, Qwen, causal intervention, shadow enforcement, or provider spend |
| A053 | 2026-07-26 | Pre-outcome restricted-boundary feasibility correction | A human-only review attempted to place three shared semantically complete dividers in the exact full, structural-sham, and inert materials. Pinned-token compilation showed that no three such shared boundaries exist. No agent inspected plaintext, no target outcome exists, and no paid GPU pod exists | Replace the infeasible semantic-block dose rule with an automated, deterministic whole-token rule: among indices that end at a whitespace-or-stronger boundary in all three materials, choose the nearest index to each nominal quarter, breaking ties by structural-boundary quality and then the smaller index. Freeze cumulative counts `64`, `128`, `188`, and `252` and bind private compiled-block SHA-256 `981e577b292c9b34ea0ddcd3eeafb57655692fbec846dbdd0abd7fd8458c0bba`. Require exact full/sham/inert token-count matching and describe partial doses only as whitespace-aligned lexical prefixes, never semantic-component ablations | This preserves the user's injection-size control and avoids word/token truncation without pretending the restricted materials share a semantic decomposition that they do not. The correction is prospective, deterministic, outcome-independent, and fail-closed | Existing scientific-scope approval and the user's explicit request to repair the restricted files mechanically without exposing their plaintext. A053 authorizes no target generation, detector fitting, causal intervention, Qwen work, shadow enforcement, or provider spend |
| A054 | 2026-07-27 | Pre-outcome core-readout and scoring-stage operational freeze | The result-free private material source and 962-condition private topology were built from exact pinned inputs; no factorial target outcome or task-owned pod exists. Local inspection found three remaining executable ambiguities: the eight-feature weights were not copied into the factorial plan, the one-number J-lens summary did not name its source layer/probe manifest, and generation receipts incorrectly required later semantic utility judgments | Freeze the inherited normalized eight-feature weights and exact float32 SAE formula; define the core J-lens summary as the layer-30 fitted-Jacobian refusal-minus-compliance margin using the exact inherited probe-plan hash and manifest; freeze the assistant boundary and refusal parser; stage HarmBench and semantic benign completion/utility in later blinded hash-bound score receipts; define the eight-condition legacy canary as two exact prior discovery requests crossed with base/full and both placements, using the duplicate base passes as the deterministic rerun; and prospectively cap SAE relative reconstruction error at `1.0` with cross-condition range at `0.25` | These changes remove implementation degrees of freedom before the first new factorial outcome, preserve the canonical estimand, and prevent unrun judges from being silently replaced with heuristics merely to satisfy a generation schema | Existing full scientific-scope approval. A054 still authorizes no provider spend or target generation; an exact canary source/rate/wall-time/cost amendment remains mandatory |
| A055 | 2026-07-27 | Exact eight-condition factorial assay authorization | A054's executable GPU path and exact private/public inputs are committed and pushed; all 196 local tests plus lint and strict plan validation pass; fresh RunPod inventory reports no pods; the retained task volume is `u85xfo0aue`; and the secure `US-CA-2` B200 catalog rate is `$5.98/hour` with low stock. No factorial target outcome exists | Authorize exactly one secure `NVIDIA B200` pod in `US-CA-2`, attached at `/workspace` to the retained volume, for run `factorial-assay-a055-20260727`. Run only the eight frozen legacy assay evaluations using exact source `e5e6fe68b05ddbab0bf784d873f205edca4b3b3c`; prohibit fallback, pooling, threshold fitting, canonical outcomes, and completed-receipt overwrite. Cap live time at 20 minutes, compute at `$1.9933333333333334`, and no-progress time at 8 minutes; require immediate teardown after receipt retrieval | The assay tests deterministic replay, exact two-sided placement rendering, SAE reconstruction stability, J-lens moment equivalence, and qualitative recovery of the already-observed feature direction before opening the new factorial cells. Charging the full cap yields a conservative infrastructure ceiling of `$88.91900617153334`, below the `$100` soft gate and `$200` hard ceiling | Existing full scientific-scope approval plus the author's repeated execution approval and 2026-07-27 instruction to proceed. The soft gate is not crossed. A055 authorizes the assay only; canonical, dose, scoring, intervention, Qwen, and deployment stages remain closed |
| A056 | 2026-07-27 | Exact 422-condition canonical factorial authorization | A055 passed all 8/8 noninferential conditions in 8.60 seconds: exact identities and renders, deterministic base reruns, vocabulary-moment equivalence, both placement-specific qualitative orderings, maximum SAE reconstruction error `0.64065`, and reconstruction range `0.21587`. The public receipt SHA-256 is `50452be9fa1e01922f8f55d219afe2ecad9b7db1498c42f9abd767cac268c987`; the locally verified private bundle SHA-256 is `f68463f696728fd6f4459db39409dbcf26e62dc3e75f3dfa579d0675d822dbe9`; and assay pod `3o2tuohbycfwo0` is terminated | Authorize the exact 422-condition canonical matrix only, using the same public/private inputs, exact source `e5e6fe68b05ddbab0bf784d873f205edca4b3b3c`, passed assay receipt, one secure B200 in `US-CA-2`, and retained volume. Preserve request-class, material, and placement separation; prohibit size pooling, threshold fitting, later scoring, dose cells, and overwrite. Cap live time at 60 minutes, compute at `$5.98`, and no-progress time at 12 minutes, with atomic per-trial resume and immediate post-run teardown | The 60-minute wall is 2.10 times the prior empirical 28.59-minute projection for 422 generation/capture observations and allows a recoverable stop rather than relaxing completeness. Charging the full cap yields `$94.89900617153334`, below the `$100` soft gate and `$200` hard ceiling | Existing full scientific-scope approval and the author's repeated execution instructions, including the 2026-07-27 full-throttle go-ahead. The soft gate is not crossed. A056 authorizes canonical generation/core receipts only; behavioral scoring, dose, threshold, causal, Qwen, and deployment stages remain closed |
| A057 | 2026-07-27 | Post-matrix descriptive-sentinel receipt-schema correction | A056 completed all 420 inferential matrix cells first-attempt with zero failure receipts, then stopped on the first of two separately descriptive literal sentinels. The private plan gives those sentinels `render_group_sha256=null` because they have no cross-material comparison group, but the generic injected-receipt validator required a non-null group. The rejected attempt committed no receipt; the second sentinel was never run; no raw outcome or analysis was opened; and the pod was terminated before its cap. The immutable 420-receipt manifest SHA-256 is `edf564767a91238fe96a8fcd36989b175ad0cb2405acd2755a336bc1934c39b7`; the preserved failed-attempt artifact SHA-256 is `98c064f1d93b9ebf55c0dbde0f0c8576df14e74d1eedaf663863e16e3ce7e793` | Correct only the receipt invariant: injected non-sentinel cells must retain a non-null cross-material render-group hash, while `literal_sentinel` receipts must use the already-frozen null hash. Add a separate two-sentinel runner that validates all 420 predecessor receipts and their exact old source/run provenance, writes into a new output root under a new source/run identity, keeps both placements separate, and never rewrites or re-sources the matrix. The repair remains descriptive-only and may not fit thresholds, pool conditions, score behavior, or enter the inferential matrix | The null is not missing data for these n=1 sentinels; it is the planned representation of “no matched cross-material group.” A separate provenance lane prevents a post-outcome schema correction from silently changing the source identity of the 420 completed cells. Focused synthetic tests pass for null-sentinel acceptance, non-null-sentinel rejection, exact 420-receipt locking, two-placement repair, and separate receipt resume | Existing full execution authorization permits the local infrastructure correction. A057 authorizes no provider spend or new model outcome; a separate exact source/rate/wall/cost amendment is required for the two missing descriptive sentinels |
| A058 | 2026-07-27 | Exact two-sentinel repair execution authorization | A057's sentinel-only runner is committed and pushed at exact source `13e19efb91e630da88b12b1f54ef1b62a37ef25b`; all 200 tests and lint pass; no task pod exists; and the 420-receipt matrix remains locked at manifest `edf564767a91238fe96a8fcd36989b175ad0cb2405acd2755a336bc1934c39b7` under old source/run provenance | Authorize only sentinel trial IDs `b3bfaa7906c6895978a7796d` and `c48ebe0f4166fdb55ded1fba`, separately representing before/after placement, into a new output root. Require exact validation of all 420 predecessor receipts before model load; forbid rewriting, re-sourcing, or analyzing them. Use one secure B200 in `US-CA-2`, the retained volume, exact source `13e19efb91e630da88b12b1f54ef1b62a37ef25b`, a 15-minute wall, 5-minute no-progress stop, and `$1.495` compute cap | Two maximum-length 1,024-token generations project below two minutes at the frozen slow rate, leaving ample time for model/lens/SAE load, 420-receipt validation, retrieval, and teardown. Charging the full cap yields a conservative `$96.39400617153334`, below the `$100` soft gate and `$200` hard ceiling | Existing full scientific/execution approval and the 2026-07-27 full-throttle instruction. The soft gate is not crossed. A058 authorizes only the two descriptive sentinels; analysis, scoring, dose, threshold, causal, Qwen, and deployment stages remain closed |
| A059 | 2026-07-27 | Pre-model sentinel omitted-key repair | A058 stopped before runtime construction because the exact private sentinel rows omit `render_group_sha256` rather than storing JSON `null`; zero sentinel receipt was written and the existing 420-cell matrix remained unchanged | Canonicalize only missing-or-null sentinel render-group hashes to JSON `null`, retain non-null enforcement for all non-sentinel injected rows, and retry the same two frozen sentinel trial IDs in the same task-owned pod under a new run and source identity | Missing and explicit null encode the same prospectively intended absence of a cross-material render group for descriptive sentinels. The correction changes no prompt, readout, estimator, placement, or result | Existing full execution approval and fail-closed same-stage recovery. The retry retained A058's remaining wall/cost scope and authorized no matrix rewrite, analysis, scoring, dose, threshold, causal, Qwen, or deployment work |
| A060 | 2026-07-27 | Exact 540-condition scaffold-size dose authorization | The canonical 420-cell matrix plus two descriptive sentinels is complete, frozen, analyzed, reported, and bound by execution receipt `d9a0e0e2ac3dadb9cd2a943d7d072f1f806045198f95769e9f2ee91736ce620b`; the dose runner is committed at exact source `4b5bda248e99b34d4394365b19cc0cf666c295da`; 210 tests and Ruff pass; provider inventory is empty; and the secure B200 catalog rate is `$5.98/hour` with low stock | Authorize exactly 540 new partial-dose observations: injected token counts `64`, `128`, and `188`, crossed with three frozen materials, three request classes, two placements, and ten frozen requests per class. Reuse the 180 existing 252-token cells without regeneration. Bind the exact prior matrix manifest, canonical result, execution receipt, private/public plans, assay, and dose-observation manifest. Prohibit placement or size pooling, threshold fitting, held-out confirmation, behavioral scoring, fallback, and completed-receipt overwrite. Use one secure B200 in `US-CA-2`, retained volume `u85xfo0aue`, 75-minute wall, 12-minute no-progress stop, and `$7.475` compute cap | The empirical projection is 2,195.91 seconds; the wall is 2.05 times that projection and preserves atomic resume. Charging the full cap yields a conservative `$105.36400617153334`, above the `$100` soft gate but below the `$200` hard ceiling | The author's explicit full-program approval, specific injection-size-control requirement, 2026-07-27 instruction to proceed, and subsequent direct instruction to do it constitute renewed soft-gate approval. A060 authorizes the secondary dose generation/core-readout stage only; scoring, threshold calibration, causal intervention, Qwen, and deployment remain separately gated |
| A061 | 2026-07-27 | Exact no-fallback H200 replacement for unallocated A060 | Three exact A060 B200 creation requests returned no available instance and no pod ID; provider inventory remained empty and no GPU charge or dose outcome exists. Read-only US-CA-2 inventory reports H200 capacity; the current H200 rate is `$3.59/hour`; the prior measured factorial peak is 22.74 GB | Supersede the unexecuted A060 run identity with exact source `1e458fcf9ded89e68aee3e4949f2815848ff9cb7` and exactly one secure 141 GB `NVIDIA H200` in the same US-CA-2 datacenter, same pinned CUDA 12.9 image, same retained volume, same 540 rows, same hashes, same 75-minute wall and 12-minute no-progress stop. Permit no GPU list or automatic fallback. Keep every scientific and raw-outcome boundary from A060 unchanged | H200 memory exceeds the measured peak by more than sixfold and changes neither model weights, tokenizer, SAE, lens, prompt bytes, decoding, observation topology, estimand, nor analysis. The exact new cap is `$4.4875`, yielding conservative post-run ceiling `$102.37650617153334`, lower than A060 and below the `$200` hard ceiling | Existing renewed full-program and soft-gate approval. A061 authorizes the secondary dose generation/core-readout stage only; scoring, threshold calibration, causal intervention, Qwen, and deployment remain separately gated |
| A062 | 2026-07-27 | Exact no-fallback H100 SXM replacement for unallocated A061 | Two exact A061 H200 creation requests returned no available instance and no pod ID, including one without unnecessary host-allocation constraints; inventory remained empty and no GPU charge or dose outcome exists. The catalog reports H100 SXM at `$2.69/hour`; the prior measured factorial peak is 22.74 GB | Supersede the unexecuted A061 run identity with exact source `296fdde7a9b3422b09095f0c6d8bd55d0df969ef` and exactly one secure 80 GB `NVIDIA H100 80GB HBM3` in US-CA-2, with the same pinned CUDA 12.9 image, retained volume, 540 rows, hashes, 75-minute wall, and 12-minute no-progress stop. Permit no GPU list or automatic fallback. Keep every scientific and raw-outcome boundary from A060 unchanged | H100 memory exceeds the measured peak by more than threefold and changes no scientific input or computation contract. The exact cap is `$3.3625`, yielding conservative post-run ceiling `$101.25150617153334`, below both superseded caps and the `$200` hard ceiling | Existing renewed full-program and soft-gate approval. A062 authorizes the secondary dose generation/core-readout stage only; scoring, threshold calibration, causal intervention, Qwen, and deployment remain separately gated |
| A063 | 2026-07-27 | Pre-model A062 live-rate reconciliation | A062 allocated task pod `dxbw98m5wi6ifn`, but the create response returned `$2.99/hour`, above the frozen `$2.69/hour` maximum. The pod was deleted immediately before SSH, source staging, model load, or any outcome; post-delete inventory is empty | Retain exact source `296fdde7a9b3422b09095f0c6d8bd55d0df969ef`, H100 SXM, image, datacenter, volume, 540 rows, hashes, 75-minute wall, 12-minute no-progress stop, and all scientific boundaries. Raise only the maximum live rate to the observed `$2.99/hour`, the compute cap to `$3.7375`, and the conservative post-run ceiling to `$101.62650617153334` under a new run identity | The provider price response changed no scientific input or execution topology. Prospective reconciliation preserves the fail-closed cost gate and remains below the `$200` hard ceiling | Existing renewed full-program and soft-gate approval. A063 authorizes the secondary dose generation/core-readout stage only; scoring, threshold calibration, causal intervention, Qwen, and deployment remain separately gated |
| A064 | 2026-07-27 | Zero-outcome probe-plan path correction | A063 exact preflight passed, then the runner wrote zero trial receipts, zero restricted artifacts, and 27 failure receipts with one error hash before being stopped. Static source resolution maps the hash to `factorial probe source-plan hash drift`: the invocation supplied `plans/followup_v2.public.json`, while the already-frozen factorial plan names `plans/study_v1.public.json` with SHA-256 `a2ed9a0542a6953dbbfd775064366e7b88a07a8f9347eb96679b0ba77300a24e` | Preserve the failed A063 output root, exact source, inputs, pod, rate, original 75-minute pod cap and teardown, and every scientific boundary. Start a new output root and run identity on the same sole task pod, changing only the invocation's probe-plan path to the prospectively pinned file. Validate its hash before model execution | The path correction removes an invocation mismatch without changing the pinned probe manifest, token IDs, model, SAE, lens, prompt, observation, readout, or analysis. Reusing the same pod stays within the original non-additive A063 cap | Existing renewed full-program approval and fail-closed same-pod recovery. A064 authorizes only the same 540-row dose stage |
| A065 | 2026-07-27 | Zero-outcome complete-model-snapshot correction | A064 passed the corrected probe hash, then wrote zero trial receipts, zero restricted artifacts, and seven failure receipts with one `FileNotFoundError` hash before being stopped. Read-only artifact audit found that the supplied model mirror held config/index files but zero weight shards; the retained pinned Hugging Face snapshot holds all four expected weight shards | Preserve the failed A064 root and every A064 source/input/pod/cost/scientific boundary. Under a new run/root on the same pod, change only `model_path` to the complete retained snapshot whose directory basename is the pinned revision. Require all four shards and config before runtime start | The model revision and bytes do not change; this corrects an incomplete local path. Reusing the same pod remains within the original non-additive cap | Existing renewed full-program approval and fail-closed same-pod recovery. A065 authorizes only the same 540-row dose stage |
| A066 | 2026-07-27 | Prospective H100 wall-time extension from receipt throughput | A065 loaded the complete model and at runner elapsed 2:35 had exact 12 trial receipts, 12 restricted artifacts, and zero failures. No outcome value or raw artifact was inspected. Receipt-count throughput showed the 75-minute B200-derived wall was too tight for the H100 | Keep the same pod, PID, source, run/root, 540 observations, inputs, stop rules, and scientific boundaries. Extend only total pod wall time from 75 to 110 minutes, with hard teardown `2026-07-28T03:51:48Z`; retain the 12-minute no-progress stop and immediate teardown after retrieval | At `$2.99/hour`, 110 minutes caps total pod compute at `$5.4816666666666665` and the conservative study ceiling at `$103.3706728382`, below the earlier B200 authorization and the `$200` hard ceiling. The change is based only on infrastructure counts, not outcomes | Existing renewed full-program and soft-gate approval. A066 changes no estimand, analysis, prompt, model, readout, or stage |
| A077 | 2026-08-09 | New prospective weaponization-detector and bounded-breaker protocol | The completed factorial showed that feature `6779` responds to the tested structured material across neutral, benign, and harmful requests and therefore does not by itself identify harmful use. The Jacobian-lens readout was descriptive and placement-dependent, the coarse causal grid stopped, the blog remains a draft, no new detector or causal outcome exists, and fresh inventory contains no task-owned pod | Freeze a new attack-scaffold versus multi-family harmless-scaffold program. Cross neutral, benign, and harmful requests with no scaffold, inert length, matched sham, at least 30 semantic families and 300 unique wrappers per harmless partition, and the existing restricted full attack scaffold. Keep before/after placement and `64`/`128`/`188`/`252` token sizes separate and exactly matched. Define weaponization from the harmful-request interaction plus a frozen unsafe-behavior endpoint. Compare feature `6779`, the frozen subspace, placement-specific J-lens heads, exact/fuzzy restricted lexical baselines, and a structural parser. Freeze one candidate and threshold per placement before a one-shot panel with 300 critical negatives per stratum. Permit only offline, shadow, and local-canary breaker modes; the action cancels one request before release and never terminates the server. Add finer token/component difference-in-differences patching with benign-utility gates. Keep restricted text outside the research agent's context | The missing construct is not scaffold presence but harmful use. Same-scaffold harmful-versus-benign and same-harmful-request attack-versus-harmless comparisons isolate that interaction. Large structured-benign panels test whether a detector merely recognizes formatting. A two-key breaker can be useful as a policy mechanism even if the monitored coordinates are correlational, while separate causal tests prevent performance from being mislabeled as mechanism | The author's 2026-08-09 directive to gather more data, compare harmless and harmful scaffolds, and pursue short-circuiting supplies scientific-scope and local-implementation approval. A077 authorizes no provider spend or target outcome. Paid stages still require exact source, live rate, wall time, no-progress stop, worst-case cost, fresh inventory, and a conservative total below the `$200` hard ceiling |
| A078 | 2026-08-09 | Pre-outcome harmful-panel source and harmless-library binding | A077 is committed and pushed at `4f3a87f`; no new target outcome or task pod exists. The official HarmBench repository was pinned without printing behavior text, and a safe public harmless-wrapper library was compiled locally with the pinned Llama tokenizer | Bind HarmBench revision `8e1604d1171fe8a48d8febecd22f600e462bdcdd`, CSV SHA-256 `8d81accedd38eaaf8b760618622bb888417d1fd0c86eba65c427a16f1cbb4afc`, an outcome-blind `80` standard plus `20` contextual calibration allocation, and a disjoint `120` standard plus `30` contextual confirmation allocation. Exclude every predecessor request hash. Bind safe library SHA-256 `46d5697068e3de551cd4a1ef06d2112248e4e66c4b613365d72f61340a8e0276`, with 15 disjoint semantic families and 20 variants per partition, and exact cumulative sizes `64`/`128`/`188`/`252`. Require 300 disjoint locally generated benign and neutral requests per partition before W2 | The fixed source supplies enough new harmful requests for threshold calibration and one-shot confirmation without reusing the panels that selected feature `6779`. Functional and semantic stratification prevents an accidental one-category split. The safe wrapper library supplies 600 unique structure-heavy negatives while leaving human semantic attestation as an explicit pre-generation gate | Existing A077 scope approval. A078 authorizes source retrieval, private hash-only panel construction, and local tests only; no target generation, behavioral scoring, or provider spend |
| A079 | 2026-08-09 | Pre-outcome harmful-panel allocation repair after hash-only overlap audit | A078's official-source hash and schema checks passed, but private panel construction stopped before writing any panel because predecessor exclusion removed nine standard HarmBench rows and source deduplication removed seven contextual duplicate-text rows. Hash-only counts left exactly 191 unique standard and 93 unique contextual rows; no request text or model outcome was printed or inspected | Use all 191 remaining standard rows and 59 of the 93 remaining contextual rows while preserving the frozen 100/150 partition sizes and a near-identical functional mixture: calibration `76` standard plus `24` contextual, confirmation `115` standard plus `35` contextual. Retain semantic-category largest-remainder quotas, hash-order selection, predecessor exclusion, cross-partition uniqueness, source revision/hash, and every downstream gate | The original 200-standard requirement was infeasible after the prospectively required overlap exclusion. The revised `76%`/`24%` calibration and `76.7%`/`23.3%` confirmation mixtures are determined solely by source topology and preserve comparable functional composition without reintroducing selected predecessor requests | Existing A078 local source-construction authority. No target generation, behavioral scoring, provider spend, or detector outcome is authorized |
| A080 | 2026-08-09 | Pre-outcome complete request-panel and harmless-preview hash freeze | A079 completed the harmful request allocation with 100 calibration and 150 confirmation rows, zero predecessor overlap, and no target outcome. The deterministic safe-panel generator and pinned-tokenizer harmless compiler also completed locally | Bind harmful public receipt SHA-256 `72ea00d0dd8710686fbb66ac5f43f9693b79b66054582b932900d6eec98305e5` and private-panel SHA-256 `1e6d125cbfec4b083f0e33457bf63762f418fa136f9a7f152e94231ce38ffb66`. Bind 300 neutral plus 300 ordinary-benign requests in each partition, all disjoint and deterministically judged, at public receipt `329e25bd6378456cbe722791a90ab267eee0acb0c3a5092171a96175ce02dc52` and private hash `1c934ee4de493955afe7aa723197a14861447a804f93f4f7d2cfbc44b860832a`. Bind the 600-wrapper exact-size preview at receipt `2699e2d0c62fb8d6e01f7a475ac965b937cf86adc0704360f6090b02a757da47` and private hash `f035e4a57529c418eb2227bd218d9ad083357d26fbb8271f8e6103274f2f88b9`, while explicitly retaining `human_semantic_review_approved=false` as a fail-closed target-generation gate | This completes the outcome-blind independent-unit topology and utility oracle before runner construction. Binding the unreviewed harmless preview without treating it as approved allows receipt and resume implementation to proceed while preventing accidental target execution | Existing A079 local construction authority. A080 authorizes runner/schema implementation and synthetic tests only; target generation and provider spend remain closed until human semantic attestation and a separate exact-cost authorization |
| A081 | 2026-08-09 | Pre-outcome confirmation-independence repair and second harmful source | The A080 topology had 300 negative wrappers per confirmation stratum but only 150 harmful confirmation requests. Pairing two wrappers to each harmful request would make 300 rows while leaving only 150 independent request families, invalidating the planned sub-1% false-positive reference. No target outcome or task pod exists | Add the official AdvBench CSV already bundled at the same pinned HarmBench revision, SHA-256 `0e635254df9065ae6f8d074534b660fa7a361d1ad269e2c8e1b7377961802a86`. Freeze 100 calibration requests (`47` standard HarmBench, `23` contextual HarmBench, `30` AdvBench) and 300 confirmation requests (`144`, `70`, `86` respectively), excluding predecessor and cross-source duplicates. Raise confirmation positive attempts per placement from 150 to 300. Bind the rebuilt harmful receipt SHA-256 `5406548504147c00f989d9fe1cc170c95d441d8bf0cef3784a01dacfc4fec261` and private hash `09a181aa55d2a0ae6fb1d7de4eabaec5501ce42a3f88c682961df5ccf29c5372` | Every attack-versus-harmless confirmation comparison can now pair one distinct harmful request with one distinct harmless wrapper. The repair prevents repeated requests from masquerading as independent evidence and makes the 300-row false-positive gate interpretable for the pinned panel | Existing A080 local source-construction authority. No target generation, behavioral scoring, provider spend, or detector outcome is authorized |
| A082 | 2026-08-09 | Pre-outcome placement-separated detector topology preview | A081 supplied enough independent request and wrapper families. No target outcome or task pod exists, and the harmless semantic-review flag remains false | Mechanically render the canonical 252-token calibration and confirmation topologies using the pinned tokenizer and existing restricted attack/sham/inert blocks without printing them. Calibration contains 1,900 prefill-only observations: seven primary strata and one secondary harmless-neutral stratum at 100 requests × two placements, plus three 100-request shared no-scaffold strata. Confirmation scales the same topology to 5,700 observations with 300 request families. Bind calibration receipt `7216438a5b258c2cfd77d582bbd01b0000c4cffc62bc00242acfc05f648e2fc2` and private hash `329ece5de806f7e49f30f20db8f54c894816a36eba25c358dc4484e35d42aecf`; bind sealed confirmation receipt `4f8eabf14807b7ba550e5215f8aa56f3c494decb049ff44373da8f03f6bd52e0` and private hash `6a18571845b5ff75830af886d1abe08f10dceac49dba350a15dbe25667b66a87`. Both bind non-circular protocol SHA-256 `29d784853d030e639ce133b4ade0e669c6e19d241d1f9dd050e10f28416ce7df`. Mark both as previews with target execution closed | The preview proves the exact independent-unit, placement, material, request, prompt-hash, and token-count topology before GPU code or cost authorization. Keeping it execution-ineligible permits runner and resume testing without bypassing human review | Existing A081 local implementation authority. No target model execution, provider spend, or confirmation opening is authorized |
| A083 | 2026-08-09 | Pre-outcome prefill runner and crash-resume gate | Both A082 topologies remain outcome-free previews, current RunPod inventory is empty, and no local target runner exists | Bind the new receipt-only GPU prefill implementation: all 31 placement-separated J-lens source layers, Goodfire layer-19 feature `6779`, the frozen eight-feature subspace with exact weights, SAE reconstruction error, restricted exact matching, structural baselines, per-row latency/memory, immutable mode-0600 receipts, and a crash-resume manifest. The runtime is lazy so no model or restricted material loads before topology and authorization validation. Bind the stable protocol hash independently from topology receipt hashes to remove a circular provenance dependency. Add the explicit harmless-control human-review checklist | The calibration must be restartable without duplicate forwards or raw logging, and every numeric readout must trace to exact model, SAE, lens, probe, topology, and source bindings. A human-readable review gate prevents a syntactically safe but semantically unsuitable control from silently entering target execution | Existing A080 local implementation and synthetic-test authority. No target forward pass, provider spend, behavioral generation, calibration analysis, threshold fit, confirmation opening, or deployment is authorized |
| A084 | 2026-08-09 | Human harmless-control attestation and final topology freeze | The study author explicitly approved immutable library SHA-256 `46d5697068e3de551cd4a1ef06d2112248e4e66c4b613365d72f61340a8e0276` for target-model execution. No task pod or target outcome exists | Bind public attestation receipt `b70e42e05f471e3aabbd513da61d6e7dab14c857b60646c70e857cf4dfb05b1b`, reviewed wrapper receipt `c127812c8b0dcae5bb479dd944905c904423bd464408ea02dac5c53a198b88b0`, and private reviewed material `ed7583efb9a58380bbaf15d9e05f7e76e1d7a8a4ab665191f8ac455ececa947e`. Freeze calibration topology public/private hashes `c240fd8de3a24a1ad3cc51d175afbf0b698673e7aed8cb0e2e21d1f300a990dd` / `5d5cfb80e39d3e53facc7b2b6f11b808b4d9aa35a3c5ca98a12fa5064bfa37e3` and sealed confirmation hashes `1fe0c10fffb62e1fd0b8db8869fd9eec5cb1d8746b5584faadd2ccce648989fa` / `bdbcf321b55e1949516465ffa4fd339c940fbf0e8600ad75750a25ead2ace5bd`, all bound to protocol SHA-256 `7574640bc6fb8f088fe3a793757faa1e7c348845dd89620f77608e3759b6b267` | An external attestation receipt preserves the exact library bytes the human approved. Recompilation changes only review provenance, not wrapper texts, and final topology hashes ensure the reviewed material rather than the preview is the only execution-eligible input | The author's exact 2026-08-10 UTC attestation authorizes target-model execution of these harmless controls under the frozen protocol. Provider spend still requires a separate exact source/rate/wall/no-progress/cost authorization; confirmation remains sealed |
| A085 | 2026-08-09 | Calibration-only paid prefill authorization | A084 is committed and pushed at exact source `956431020f2cd135542adbeb4e4e94bdb0d314ca`. Read-only provider inventory at `2026-08-10T01:11:22Z` reports no task-owned pod, two unrelated running pods that are excluded from scope, retained volume `u85xfo0aue` in `US-CA-2`, and low-stock secure H100 SXM capacity at `$3.29/hour` | Authorize exactly one secure `NVIDIA H100 80GB HBM3`, one GPU, exact retained volume and pinned CUDA image, no fallback, batch size four, 1,900 calibration prefill observations only, 120 minutes maximum wall, 15-minute no-progress stop, and at most two attempts per trial. Maximum new compute is `$6.58`; conservative infrastructure envelope becomes `$109.9626703185`, below the `$200` hard ceiling. Bind plan `d2309e1d...`, private topology `5d5cfb80...`, public topology receipt `c240fd8d...`, protocol `7574640b...`, review receipt `b70e42e0...`, and probe plan `a2ed9a05...` | The exact run tests the harmless-versus-attack interaction without generation or confirmation opening. A two-hour cap covers the slower prior 8B throughput envelope while atomic receipts and a 15-minute no-progress stop bound failure exposure | The author's A084 target-execution approval plus the standing doubled `$100/$200` budget approval. A085 authorizes calibration prefill readouts only. It does not authorize calibration analysis before retrieval/binding, threshold rescue, behavioral generation, causal intervention, confirmation, active enforcement, or production deployment |
| A086 | 2026-08-09 | Exact H200 substitution after two unallocated H100 requests | Two exact A085 secure H100 creation requests returned `no instances available`, each with no pod ID; fresh inventory confirms no task-owned pod or charge. The only unrelated pod remains excluded. Read-only US-CA-2 inventory reports secure H200 at `$4.59/hour`, low stock, with 141 GB memory | Supersede only the unexecuted GPU type and rate: authorize one secure `NVIDIA H200`, exact source/input/image/volume/datacenter, batch size four, 1,900 calibration rows, 120-minute wall, 15-minute no-progress stop, no fallback. Maximum compute becomes `$9.18`; conservative envelope becomes `$112.5626703185`, below the `$200` hard ceiling | H200 memory exceeds the frozen requirement and changes no model, prompt, tokenizer, SAE, lens, readout, receipt, analysis, or confirmation boundary. A prospective single-GPU substitution preserves both no-duplicate and cost controls | Existing A084 target-execution and standing `$200` ceiling approval. A086 authorizes calibration prefill only and inherits every non-authorization from A085 |
| A087 | 2026-08-09 | Exact B200 substitution after unallocated H200 request | The sole exact A086 H200 creation request also returned `no instances available` with no pod ID; fresh inventory still shows no task-owned pod. Secure B200 is the only advertised compatible US-CA-2 device, at `$6.79/hour`, low stock, with 180 GB memory | Supersede only the unexecuted GPU type and rate: authorize one secure `NVIDIA B200`, exact source/input/image/volume/datacenter, 1,900 calibration rows, batch size four, 120-minute wall, 15-minute no-progress stop, and no fallback. Maximum compute becomes `$13.58`; conservative envelope becomes `$116.9626703185`, below the `$200` ceiling | B200 was used successfully in predecessor stages, has ample measured memory margin, and changes no scientific input, computation, readout, receipt, or stage gate. The higher two-hour cap remains a small fraction of the authorized remaining envelope | Existing A084 target-execution and standing `$200` ceiling approval. A087 authorizes calibration prefill only and inherits every non-authorization from A085 |
| A088 | 2026-08-09 | Zero-outcome chat-template serialization repair | A087 loaded the exact model and then stopped before the first model forward or trial receipt. Type-only inspection found that the newer tokenizer API returned a mapping for all 300 shared no-scaffold rows; the topology builder converted that mapping to string keys rather than extracting integer `input_ids`. All 1,600 injected rows had integer token IDs. No result or restricted outcome was produced or inspected | Normalize mapping, tensor-like, flat-list, and one-row batched chat-template outputs into exactly one non-empty integer sequence; reject every other type or shape. Rebuild both calibration and still-sealed confirmation topologies from the same reviewed inputs and pinned tokenizer. Preserve the failed A087 root. Require a new exact source/topology/plan authorization before a same-pod restart | This repair restores the intended no-scaffold controls without changing requests, scaffold materials, model, SAE, lens, readouts, analysis, or confirmation boundary. Because prompt-token hashes and trial IDs change for the 300 repaired calibration rows and 900 sealed confirmation rows, all affected topology hashes are replaced prospectively rather than treating the issue as a runtime cast | Existing target-execution approval covers the reviewed harmless library, but not an unrecorded retry. A088 authorizes local repair and validation only; paid execution requires a subsequent same-pod authorization within A087's original wall and cost cap |
| A089 | 2026-08-09 | Repaired calibration same-pod execution authorization | A088 is committed and pushed at exact repaired source `614ee925da9a3a46f573cb83265ad0813943bd05`. Full tests, lint, strict plan validation, and a type-only 1,900-row token audit pass. Fresh provider inventory reports exactly one running account pod: task pod `s2d7s05f3r7n7n`; A087 has zero trial receipts and no active runner | Authorize a new run/root on the same exact B200 pod only, binding source `614ee92...`, plan `884f4187...`, repaired calibration private/public topology `d21970ef...` / `9f8abb8b...`, stable protocol `7574640b...`, review receipt, and probe plan. Preserve the original `$6.79/hour`, 120-minute creation-to-teardown wall, `2026-08-10T03:20:02Z` hard teardown, 15-minute no-progress rule, and `$13.58` maximum compute from original creation. Do not create another pod | The failed attempt emitted no outcomes, and this new prospective identity cleanly separates the serialization repair from scientific acquisition while keeping cost non-additive. The repaired no-scaffold rows are necessary controls; running the stale topology would invalidate the experiment | The author's exact harmless-library target-execution approval plus the standing `$200` ceiling and direction to proceed. A089 authorizes only repaired calibration prefill on the already-running task pod. Confirmation, generation, analysis before complete binding, causal intervention, and active enforcement remain unauthorized |
| A090 | 2026-08-09 | Zero-outcome deterministic cuBLAS environment qualification | A089 passed repaired source/input validation, then stopped during runtime initialization with zero trial receipts and before a model forward. Torch deterministic mode rejected the output-embedding matrix multiply because CUDA cuBLAS requires an explicit workspace configuration. A synthetic 8-by-8 GPU matrix multiply passed with `CUBLAS_WORKSPACE_CONFIG=:4096:8`, without loading model or prompt data | Start a new run/root on the same sole task pod and exact A089 source/input bindings, adding only the qualified `CUBLAS_WORKSPACE_CONFIG=:4096:8` process environment. Preserve A087 and A089 roots, original pod wall/cost cap, and every scientific boundary | The environment variable activates CUDA's documented deterministic cuBLAS workspace path; it changes no model, inputs, readouts, analyses, or trial topology. Prospective recording prevents a silent operational change | Existing target-execution and budget approvals. A090 authorizes only repaired deterministic calibration prefill on exact task pod `s2d7s05f3r7n7n`; all A089 non-authorizations remain in force |
| A091 | 2026-08-09 | Outcome-blind calibration analysis and threshold freeze | A090 completed 1,900/1,900 prefill receipts, its local private bundle matches remote SHA-256 `ac729334...`, and exact task compute is torn down. No receipt outcome was inspected before source `8ad5e06389a7abf91ae2ca26ba07324aa37501cc`, analysis plan `fa7a667d...`, 288 tests, and lint were committed and pushed | Authorize one local analysis invocation against the exact bound bundle. For each placement separately, fit a fixed 31-coordinate class-balanced ridge-logistic J-lens weaponization key; combine feature `6779` and the frozen subspace by coefficient-free empirical midranks for the structure key; require both nondegenerate thresholds; apply the frozen 0.90 recall and per-stratum 0.02 false-positive gates; and report exact, fuzzy, structural, feature, subspace, and J-lens comparators. Emit a public aggregate result and mode-0600 private candidate artifact | The protocol named the two keys and gates but did not fully specify standardization, optimizer, rank combination, threshold enumeration, or deterministic tie-breaking. Freezing those choices before reading the complete bundle prevents outcome-driven rescue and yields a candidate artifact that can be applied unchanged to sealed confirmation | The author's continuing full-program approval. A091 authorizes calibration analysis only, with no paid compute. Confirmation remains sealed until the result and candidate hash are committed and a new prospective authorization exists |
| A092 | 2026-08-09 | Calibration pass and candidate freeze | The single A091 invocation completed against all 1,900 bound receipts. The primary two-key candidate passed both placements: before-request recall `100/100`, after-request recall `92/100`, and `0/100` false positives in every one of six critical-negative strata per placement. Feature `6779`, the frozen SAE subspace, exact match, fuzzy match, and the structural head each failed as standalone gate candidates; the J-lens head was the only eligible standalone comparator and had calibration AUROC `0.9996` before and `0.9965` after. Confirmation remains unopened | Freeze private candidate SHA-256 `df83a51d...` and public result SHA-256 `aa6d6c03...` without refitting. Generate four placement-separated receipt-backed figures for the exact gate, comparator ranking, SAE contrasts, and all-layer J-lens trajectories. Require byte-identical SVG/PNG/PDF regeneration before any confirmation authorization | The decisive calibration pattern is two-part: SAE feature 6779 remains a scaffold-form signal, while the fitted J-lens trajectory separates attack-plus-harmful from attack-plus-benign and from harmful requests under harmless or sham structure. In-sample calibration can overfit a 32-coefficient head, so the result justifies exactly one held-out test, not a defense claim | Existing staged protocol. A092 authorizes reporting and candidate freeze only. Any confirmation acquisition requires a fresh exact pod/rate/wall/cost authorization and may not refit thresholds, coefficients, rank references, placements, or controls |
| A093 | 2026-08-09 | One-shot detector-confirmation acquisition | A092 is committed/pushed at `135b9b3`. The frozen confirmation evaluator is committed/pushed at source `ecc87beed1e293cb2897509390b933bbcdbc78c0`; full tests and lint pass. Fresh provider inventory reports zero pods, retained task volume `u85xfo0aue` in `US-CA-2`, and secure A100 PCIe 80 GB list price `$1.39/hour` | Authorize exactly one secure A100 PCIe 80 GB pod on the retained volume and pinned image for 5,700 confirmation-prefill observations only, batch size four, deterministic cuBLAS environment, 90-minute wall, and 15-minute no-progress stop. Bind the exact source, repaired sealed topology, calibration result, private candidate hash, and frozen confirmation analysis plan. Maximum new compute is `$2.085`; conservative infrastructure becomes `$107.8537819407`, below the `$200` ceiling | The completed B200 calibration used only 24.5 GB and acquired 1,900 rows in under two minutes after initialization. An 80 GB A100 has ample memory; the 90-minute cap conservatively covers lower throughput while remaining inexpensive. Confirmation is one-shot and cannot be used to refit | The author's continuing full-program and `$200` ceiling approvals. A093 authorizes confirmation prefill acquisition only. Analysis remains closed until the complete bundle is locally retrieved, schema/provenance validated, and hash-bound |
| A094 | 2026-08-09 | Exact A100 SXM substitution after unallocated A100 PCIe request | The sole A093 allocation request returned `no instances available`; the response contained no pod, and fresh inventory confirmed `pods=[]`. Secure A100 SXM 80 GB is listed at `$1.59/hour` | Supersede only the unexecuted GPU SKU and rate with one secure `NVIDIA A100-SXM4-80GB`; keep exact source, sealed inputs, candidate, image, volume, 5,700 rows, deterministic environment, batch, 90-minute wall, 15-minute no-progress rule, and no fallback. Maximum compute becomes `$2.385`; conservative total becomes `$108.1537819407` | The substitute has the same 80 GB memory class and changes no model, prompt, readout, threshold, candidate, analysis, or confirmation boundary | Existing A093 confirmation authority and budget approval. A094 remains confirmation-acquisition-only and inherits every non-authorization from A093 |
| A095 | 2026-08-09 | Ordered secure-GPU allocation ladder after two supply failures | The sole A094 A100 SXM request also returned `no instances available`; fresh inventory again confirmed `pods=[]`. Calibration measured only 24.5 GB on B200, so every secure NVIDIA device with at least 48 GB has ample memory headroom for the identical runtime | Freeze a low-to-high exact SKU/rate ladder from A40 `$0.44/hour` through B200 `$6.79/hour`. Submit strictly in order, verify zero pods between supply failures, and stop at the first allocation. Allocation alone authorizes no model load: a separate exact returned-pod/rate/creation/hard-teardown authorization must be committed first. Worst-case 90-minute B200 compute is `$10.185`; conservative total is `$115.9537819407`, below `$200` | An ordered ladder prevents outcome or convenience-driven GPU selection while avoiding one commit per empty SKU. The 48 GB floor is nearly twice measured peak memory. A separate runtime freeze preserves exact provenance and cost control after the scheduler chooses a live device | Existing A093 confirmation and budget authority. A095 authorizes creation of at most one task pod and no paid model execution |

An amendment entry must distinguish:

- clarification with no change to the estimand;
- pre-outcome design change;
- infrastructure correction;
- post-outcome exploratory analysis;
- new prospective protocol version.

The Git diff, not this table alone, is the authoritative record of what changed.
