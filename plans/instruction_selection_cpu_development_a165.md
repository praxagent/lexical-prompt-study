# A165: exploratory CPU FP32 development scaffold matrix

Status: prospective; no A165 calls or outcomes. This is a separate exploratory
experiment on the 16 already-observed A164 development worlds. Preserve A164's
failed NF4 gate and its unused held-out worlds. The successful CPU reference is
motivation for this fixed run, not authorization to relabel or repair A164.

Compile and freeze all 288 cells before any model call. Reuse the original
A164 development worlds, AB/BA presentation, authoritative system-only selector,
exact wording, JSON renderer, oracle, native tokenization and all four unchanged
private materials. New A165 schemas and trial identities bind the original
canonical development plan, materials, source and this protocol. No held-out
world, alternative wording, model, precision or renderer may be selected.

The first 32 cells are contemporaneous no-scaffold controls, in the original
plan order. Require all complete, at least 29/32 strictly correct and at least
15/16 worlds with both selectors correct. If the gate fails, stop and preserve
all planned cells in coverage. If it passes, execute the remaining fixed 256
cells: four scaffolds (full, sham, replacement, inert), two placements and two
selectors for each of 16 worlds. Each original baseline trial is expanded in
fixed scaffold order full/sham/replacement/inert and placement order before/after;
all baseline trials precede these expansions. No automatic infrastructure retry,
completed-error retry, optional expansion or adaptive ordering is permitted.

Use original, unquantized CPU FP32 weights through the frozen corrected reference
loader `run_cpu_reference_audit.py`, SHA256
`f704a8a223c792ca35b7f2e0e000537fc366fd2f267dbb5d3802fe9c3ec4321e`.
Retain its original protocol SHA
`1e2a3074f282acfa4e15331b42b19e6c4ca05e786e8826a93f6dec412aeb8467`
and logging-amendment SHA
`503c9382a5f0e2105af9e9afa44b6bc034f71637514d1c83907e69a28f8cd527`.
Use the exact shared checkpoint/config/tokenizer, CPU SDPA, four threads, unchanged
seed and greedy 64-token generation. Require at least 48 GiB available RAM before
loading. An external 18,000-second deadline bounds the whole process, including
loading and validation. No GPU, API, model download or dequantized NF4 weights.

Freeze the native rendered token arrays for all 288 trials privately, with source
closure covering the A165 plan/runtime and analysis modules, reused A164 native
renderer/scorer/runtime, original engine, CPU loader, model files, configuration,
protocols and materials. Attempts/results are immutable and private. SIGTERM must
bypass per-trial error recovery and stop the experiment; retain partial results,
interrupted attempts and all unresolved cells. Receipt-backed numeric export
recomputes native prompts, decodes response tokens and re-scores before analysis.

Report descriptive paired full-minus-sham strict accuracy averaged over four
selector/placement descendants within each world, secondary placement and
replacement/inert contrasts, exact/other-selector/other-answer/format/cap counts,
baseline competence and all planned-cell coverage. Cap terminations are strict
failures. Infrastructure failures remain unresolved with worst/best binary bounds.
These 16 development worlds were already observed and motivated the precision
choice: no confidence intervals, bootstrap equivalence, confirmatory null or
held-out/general-task claim. A simultaneous full/sham strict-accuracy floor at
or below 10% is a descriptive interpretation warning, not a statistical test.
CPU results do not isolate quantization from device/backend differences.
