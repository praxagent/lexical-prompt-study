# A154: size the CPU export window from measured work

Status: operational duration change after CPU26's measured throughput gate,
before a complete Stage-B feature bundle or any Stage-B fitting. No detector
family, label, threshold, split, numerical transformation or outcome changes.

CPU26 successfully qualified its exact retained runtime: Python 3.12.3,
NumPy 2.5.1, Torch 2.13.0+cu130 and Transformers 5.14.1. All five projection
matrices exactly matched the already frozen local matrix bytes. The exact
eligible specification was persisted before experimental-state processing.

After original-response parity and source indexing, the unchanged throughput
gate projected **2,604.642929622052 seconds** of remaining work against
**956.7522098291665 seconds** remaining. Its samples included 113 residual
transforms, 16 long-prefix trials, 80 base-I/O samples and eight pack-I/O
samples. The existing twice-worst-observed estimate and 180-second reserve
therefore rejected the short window. Sample transformations occurred inside
quarantine, but no complete feature bundle or fitted detector was produced.
This is an operational stop, not a scientific null result.

Exact teardown and independent inventory are verified. CPU26's rounded-up
compute cost was $0.02. The closed-operation base is now $25.64 plus cumulative
storage and subsequent actual compute, within the existing $70 round budget.

## New bounded CPU28 operation

Allocate at most one study-owned CPU pod, no more than $0.16/hour compute,
with a **$0.35 total operation cap and 3,600-second allocation deadline**.
The existing storage planning reserve is $0.10/hour; a full hour is therefore
budgeted at $0.26, below the operation cap. The worker deadline is allocation
start plus **3,300 seconds**, including startup and runtime qualification.
Preserve transfer and teardown reserves, startup/no-progress limits and the
independent guardian. Check remaining cumulative-round budget before allocation.
Use actual measured allocation-to-verified-absence cost, not the operation cap,
when closing the ledger.

Keep the same runtime qualification, matrix bytes, fixed throughput samples,
twice-worst-observed extrapolation and 180-second reserve. Do not weaken the
gate or select a favorable timing sample. Reject the new window if it still
does not fit; preserve the receipt rather than blindly retrying.

Propagate the explicitly authorized duration through every nested time limit.
Test the real reconstructed guardian/receipt writer at 3,600 seconds so inherited
30-minute wrappers cannot silently override it. In the new exporter, a local
subclass may extend the loaded recovery helper's I/O constructor to at most
3,300 seconds, retaining a deadline measured from its original start. Inherit
the existing read, byte-limit, path, hash and deadline-check methods unchanged.
Do not renew the deadline per read. Preserve the original recovery-helper file
and hash; bind this duration-only adapter through the new exporter source.
The global worker alarm still bounds the complete operation.

Use a fresh CPU28 namespace and preserve all consumed CPU26 files and receipts.
The scientific exporter may change only namespace, explicit runtime ceiling
and the duration adapter. Test unchanged scientific transformations and nested
budget propagation. Reuse the pinned probe and record actual eligible versions
before state access. Bind ownership, source/input freeze, runtime qualification,
copy hashes and independent local closure before fitting.

This is retained-data CPU work within the user's existing budget and autonomy
approval. It authorizes no new model inference, GPU, dependency installation,
paid review, raw-agent exposure, public release, confirmation access or serving
deployment, and no interaction with another project's resources.
