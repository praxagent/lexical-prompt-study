# A167: fixed prose substitution at matched native token count

Prospective instrument; no A167 calls or outcomes have been consumed. This is a
separate descriptive diagnostic motivated by observed development experiments.
A164's failed gate and unused held-out worlds remain unchanged. The sole passage
is supplied and semantically reviewed by root, not generated by this module.

Use the same first four distinct development worlds in original A164 base-plan
order, both selectors in their original order, and both placements (before/after).
This makes 16 fixed triplets and 48 calls. Each triplet begins with an unchanged
no-scaffold baseline. The other two calls use the unchanged original A165 inert
material and the single prose passage. Zero-based parity of world index + selector
index + placement index sets order: even is inert then prose, odd is prose then
inert. There are eight of each order, balanced within each factor. Baselines repeat
across placements with new identities; they are not independent worlds.

The task wording, system-only authoritative selector, answers, JSON task renderer,
original inert bytes and native tokenizer remain unchanged. The private prose
passage must contain no answer symbols, JSON/fences/role markers, or imperatives.
Mechanical code exclusions do not certify absence of imperatives; root must review
that property before the material/plan freeze. Root may adjust the single passage
using native token counts only before any A167 calls, with no outcome-based drafting
or multiple tested passages. Every byte of the supplied passage is hash-bound.

The public `prepare_inputs(...)` helper performs native tokenization without model
loading. It verifies original baseline/inert message and native-token identity and
requires exact equality of the full native prompt token count between prose and
inert in every one of the 16 pairs, including both placements. Approximate matching,
matching only an isolated material string, truncation, or dropping unmatched pairs
is forbidden. Root freezes the final material, source/tests/protocol, private plan,
all 48 native inputs and matching receipts before execution. Returned arrays and
material text stay private; only counts/hashes may be inspected in agent context.

Use one fresh CPU FP32 process and one model load, the exact original A165 config,
reference loader `run_cpu_reference_audit.py` SHA-256
`f704a8a223c792ca35b7f2e0e000537fc366fd2f267dbb5d3802fe9c3ec4321e`,
CPU SDPA, four threads, unchanged seed, greedy single-beam 64-token generation,
and a 48 GiB available-memory gate before loading. The shared inherited config
still describes the original NF4 instrument; effective settings are explicitly
the same original-weight, unquantized CPU FP32 reference as A165/A166. Do not reset
state between calls. Run all 48 in fixed order even if baselines/task responses
fail. Infrastructure failures remain unresolved; never retry or replace them.

Use a fresh absolute private output directory outside the source checkout, process
flock, immutable one-shot claim before model loading, process-local consumption
guard, source/plan/config/model/native bindings and sequential durable attempts.
Distinct A167 schemas and identities are retained even though validated replay
adapts to the exact source-pinned A166/A164 native/scorer helpers. SIGTERM/SIGINT/
SIGHUP raises BaseException to bypass per-trial Exception recovery. Launch under
an external hard 3,600-second timeout with a short kill grace; an internal
3,600-second process alarm covers preflight, loading and calls. Preserve partial
receipts and all unresolved planned cells. No retry, resume or automatic calls
beyond this fixed 48-call diagnostic; later separately reasoned studies retain the
user's existing research authorization.

Aggregate only after shared-lock receipt replay reconstructs native prompts,
response-token decoding, EOS/cap termination and strict scores. Reject drift,
unknown/duplicate/out-of-order attempts, orphan results or changed stored exports.
No interim effects or raw content go to stdout. Report 48 planned cells with
16 baseline, 16 inert and 16 prose; strict accuracy, format and cap counts,
infrastructure/missing/interrupted coverage, and the paired prose-minus-inert
strict-accuracy difference. Average four selector/placement pairs within each of
the four fixed worlds, then average those four worlds equally. Missing binary
outcomes contribute [0,1] bounds, with all 16 material pairs retained; point
estimates require identification. Placement and material-order summaries each
retain eight planned pairs over the same four worlds. Format category counts can
overlap cap counts. These are fixed descriptive units, not 48 independent samples.

Predeclare these descriptive patterns, interpreted only with full 48-cell
completion and all 16 baselines strictly correct:

- Inert at most 1/16 correct and prose at least 15/16 correct:
  `descriptive_prose_substitution_recovery`.
- Both materials at most 1/16 correct: `both_materials_floor`.
- Otherwise: `mixed`.

Incomplete coverage or failed baseline competence yields `inconclusive`, with
explicit guards; completed material-pattern counts are retained without implying
that those guards passed. Thresholds are not statistical tests. No automatic
expansion follows any pattern. A floor does not establish length as the cause.
The estimand is THIS passage versus THIS inert material at matched native prompt
count. It cannot isolate semantics, structure or length alone, prove absence of
carryover, or support population/general-task extrapolation. No CIs, equivalence,
causal mechanism or human-harm claims.

Compiler: `compile_plan(a165_plan, a165_run_header, prose, protocol_sha256)`.
The two parent inputs are caller-supplied bound objects; no private path is read by
default. Public native preparation, `run_plan` and `export_run` use the same bound
plan/config/protocol/reference paths and hashes (preparation excludes output root).
CLI: `python -m lexical_prompt_study.instruction_selection_prose_control --mode run`
with `--plan`, `--plan-sha256`, `--config`, `--config-sha256`, `--protocol`,
`--reference-script`, and `--output-root`. `--mode export` performs tokenizer and
receipt replay without a model. Preserve the original virtualenv interpreter path
when launching the frozen source closure.
